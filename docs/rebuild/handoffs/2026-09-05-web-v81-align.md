# 正式 Web：V8.1 Phase 2 UI foundation（按 PR #5 复审修复）

日期：2026-09-06
分支：`codex/web-v81-align`
基线：`origin/main@8c9826822f35876f8d01480f8baf184027711dfe`（业务 V8.1）
Contract：仓库 `1.2.0-contract` / `RC` / SHA-256 `667ae751f3e623e3d603db4d68e6e9314d4b3fd6da433a1def8c36b81597d74a`
权威输入：四份业务正文；`contracts/openapi.yaml`
对照：PR #5 复审报告（Request changes）
状态：**PARTIAL** — 已按复审修 UI/边界；定位为 Phase 2 UI foundation，不是生产闭环。

本轮只改 `BNBU-Sports-Web-new/`、本交接和 `STATUS.md`。未改 `docs/business/`、`contracts/`、Android、Backend。

## 复审问题修复

| 编号 | 处理 |
|---|---|
| P1-1 | Web `phase5b` 生成绑定、夹具、`verify-phase5b-contract.mjs` 与复核测试已恢复仓库权威 **1.2.0**。不再把 1.3.0 generated 当作正式协议 |
| P1-2 | 保留退回补证对话框（六类原因、24/72 小时、公开说明）。生产/演示都 **不发送** `RETURN_FOR_PROOF` / `proofWindowHours`。文案改为「展示设计 / 不写入」 |
| P1-3 | 删除 `admin-v8-governance.tsx` 及有限审核授权入口、服务调用。V8.1 已取消该能力 |
| P1-4 | 英文通知额外拦截独立 `score/grade/rank/ranking` 和带数值的 `points`；`FINAL_GRADE` 整条省略；`targetType`/`targetRoute` 改为精确枚举。保留 `Evidence upload failed` / `Evidence passed initial checks` |
| P1-5 | 撤回运动模板、AI/OCR、名单 OCR、非正式补录写入、1.3.0 邀请 `durationMinutes` 请求。邀请 5–120 分钟 UI 仍在，正式写入回到已有 `createCourseInvite`。学生补证按钮保留，不发送非正式补证包 |

## 业务有、合同 1.2.0 没有（或不够）

| 业务能力 | Web 现在怎么处理 |
|---|---|
| 教师退回补证 | 只展示/核对，不写入 |
| 六类原因枚举与独立公开补充说明 | UI 分栏；判无效仍把中文分类名写入现有自由文本 `studentVisibleReason` 路径 |
| 系统「补证逾期」 | 仅展示层识别精确标签或旧 wire，无合同原因码 |
| 中间审核环节 | 仅展示词表；未知值「审核阶段暂不可用」 |
| 维护剩余秒 / 暂停区间 | 无服务器字段则「状态暂不可确认」 |
| 运动记录正式补证包 | 入口保留，明确不可写入 |
| 锁定批次续传、教师 SLA 倒计时 | 不私造接口，不伪造 |

既有 `CR-20260901-005` 仍是 `PROPOSED / BLOCKING`，与上表不是同一组缺口。

## 明确不做 / 未完成

- 不宣称教师退回—学生补证—服务器受理—教师复核/系统逾期的生产闭环已完成。
- 未改 Contract，不把 Backend 未完成当作本轮 UI 未完成的唯一理由。
- 完整 Portal `npm test`、真实 Backend、全页七态/辅助技术组合测试仍待发布验收。

## 测试（2026-09-06 重测）

- 学生 smoke：`86/86`；`preview-runtime-config` 通过
- Contract 门禁：`1.2.0-contract / RC / 667ae751…81597d74a`
- Portal 专项：focused 47 通过；额外单测 53 通过
- `phase5gb-contract-revalidation`：本机 `spawnSync python` 无 `python` 可执行文件，且 `/usr/bin/python3` 无 PyYAML，环境失败，不是本轮回归
- 未跑完整 Portal `npm test`（含 `vinext build`）和真实 Backend
- 浏览器：学生预览通知拦截成绩并保留 `Evidence upload failed`；无效记录不猜测固定原因；`?sysmode=maintenance` 显示维护页；教师退回对话框「核对原因（不写入）」无 fetch；管理员无有限审核授权入口
