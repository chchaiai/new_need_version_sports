# 业务客户端开发范围调整

日期：2026-09-04。完成状态：DONE（仅开发范围文档调整，非产品验收）。

## 1. 来源与执行范围

用户明确要求删除业务逻辑中涉及的 iOS，当前不开发 iOS。此开发范围决定为 ACCEPTED；当前学生端仅开发 Android / Web，教师与管理员继续使用 Web。业务权威仍为原四份正文，不新增第五份业务来源。

- Git 根目录：`C:\Users\23328\Desktop\new_version`。
- 开始分支：`codex/latest-main-20260904`。
- 开始 HEAD：`49d992a1333294ea561923cfea0b7d25864a4d91`。
- 开始状态：工作区干净、暂存区为空。
- 已读约束与输入：根 `AGENTS.md`、`STATUS.md`、四份业务正文、业务目录入口、前次 v8.0 交接及 Contract metadata；文档范围没有更深层 `AGENTS.md`。
- 当前阶段：业务文档开发范围调整，不推进客户端或 Backend 实现。
- 允许修改：下表五个精确路径；其余路径禁止修改，尤其客户端、Contract、数据库、架构、历史交接和发布配置。
- 完成标准：业务目录无 iOS 引用，当前平台范围明确，其他业务内容保持不变；完成文档与 Git 范围检查，并更新状态和交接。

## 2. 修改文件

| 文件 | 调整 |
|---|---|
| [总业务流程](../../business/00-overview.md) | 明确当前 Android / Web 开发范围；删除通知条款中的 iOS |
| [学生业务流程](../../business/10-student-flow.md) | 标题与定位仅保留 Android / Web；删除通知条款中的 iOS；收紧“其他学生端”泛指 |
| [管理员业务流程](../../business/30-admin-flow.md) | 删除维护与恢复通知条款中的 iOS |
| [STATUS](../STATUS.md) | 记录本轮范围、证据边界及当前工作状态，区分前次更新历史 |
| 本交接 | 记录用户决定、精确范围与实际检查结果 |

保留 v8.0 业务基线版本。此修改只收紧当前客户端开发范围，不改变运动、计入、审核、期限、身份、权限或仅站内通知规则。总览“三端职责”描述学生、教师、管理员三个角色，不是三种操作系统，继续保留。教师业务流程和业务目录 README 不需要改动。

没有删除任何客户端目录或文件，也没有清理其他目录中的历史平台记载。原业务表述仍可从开始 HEAD 查询。

## 3. 执行检查与真实结果

2026-09-04 13:31:04（Asia/Shanghai）执行只读文档与 Git 检查，结果通过：

- 对整个 `docs/business/`（含隐藏及被忽略文件）检索 `ios|iphone|ipad|苹果|其他学生端`，不区分大小写，结果为 0 处；原 5 处显式 iOS 引用及 1 处“其他学生端”泛指已移除。
- 将三份修改的业务正文与开始 HEAD 作 UTF-8/LF 规范化后的精确文本比较，差异仅为本轮指定的平台替换与总览范围说明；没有修改其他正文内容。
- 五个变更文档中的 31 处本地链接、4 处链接锚点全部可解析；学生标题调整未破坏这些链接。
- Git 范围核对为四个已跟踪文件修改、一个指定 Handoff 新增，暂存区为空。Contract、教师流程、业务目录 README、客户端、Backend、架构、infra 和 tests 均未修改；全部其他已跟踪路径也没有差异。
- `git diff --check` 通过；另逐行检查尚未跟踪的新 Handoff，无行尾空白。Git 的 LF/CRLF 提示是本机换行转换提示，不是内容或空白检查失败。
- 分支与 HEAD 保持开始值，未创建提交或推送。历史产品测试数字不作为本轮通过证据。

未执行 Android/Web 构建、单元测试、Contract 生成、Backend/数据库验证、E2E 或部署检查：本轮只有文档改动，没有修改实现；文档检查不能证明产品功能通过。

## 4. 结束交接

| 必报项 | 本轮结论 |
|---|---|
| 完成状态 | DONE，仅本轮开发范围文档调整 |
| 修改文件 | 第 2 节五个文件；四个修改、一个新增，没有文件删除 |
| 执行测试与真实结果 | 第 3 节文档与 Git 检查通过；没有产品测试结果 |
| 未执行测试及原因 | 第 3 节列明；仅文档范围，不作产品验收 |
| 是否修改业务规则 | 是，仅客户端开发范围；其他业务参数与流程不变 |
| 是否修改 Contract | 否；不生成协议、不提升 Contract 版本 |
| 是否存在旧 API 引用 | 按开始 STATUS 与前次交接，原客户端旧 API 仍在；本轮不迁移，也未做全仓旧 API 审计 |
| 是否存在 Mock、TODO、空接口 | 开始文档记录既有 Mock/演示与占位目录；本轮不新增产品 Mock、TODO 或空接口，未做全仓穷尽检查 |
| 下一阶段前置条件 | 按当前 Android / Web 范围重新读取四份权威及 STATUS；后续模块修改另行授权，既有 Contract 阻塞仍须按 CR 流程处理 |

本轮不暂存、不提交、不推送、不切分支、不合并、不清理、不部署。范围文档完成不代表客户端、Backend 或真实业务闭环已实现。
