# P4-Z · V8.1 Domain / Database / Architecture 增量设计

> 状态：ACCEPTED（本批次约定深度的设计交付）；Owner：周润基；Reviewer：黄友晟。
>
> 执行状态：NOT EXECUTED（产品、Contract、数据库与 migration 均未执行）。文档算术核对单独记录，不属于产品验收。
>
> 本批次：PARTIAL；Phase 4：IN_PROGRESS；phase_5_gate：LOCKED。
>
> 范围：A 深入级，C/E 要点级，G2 见独立迁移说明。2026-09-06。

确认讨论入口：[P4-Z 问题与确认记录](#10-p4-z-问题与确认记录)（持续更新，含已确认事项与开放问题）。

## 0. 基线、授权与开始报告（设计阶段历史；归档授权见 §10.10）

- Git 根目录：`/Users/labyr1nth/Desktop/Phase4&7/new_need_version_sports`；分支 `main`；HEAD `dbdedf9be958af08dd4a2c56d23191c626cca625`，与任务指定基线一致。
- 开始 `git status --short --branch --untracked-files=all`：`## main...origin/main`，以及未跟踪的 `.DS_Store`、`contracts/.DS_Store`、`docs/.DS_Store`。发现后暂停；周润基在本会话确认允许 P4-Z 继续，三项保留不动。
- 22:43续作发现新增 `docs/architecture/.DS_Store` 后再次暂停；周润基明确答复“和此前三项一样，先不管，继续推进”，本项同样保留不动。联合收尾开始时Git仍为上述main/HEAD，两份P4-Z文件及四项DS未跟踪，暂存区与已跟踪diff为空；H附件位于仓库外工作区，仅按用户提供授权只读。
- 已读根 `AGENTS.md`；对所访问 docs、architecture、new-requirements、business、rebuild、handoffs、contracts 的逐级 `AGENTS.md` 精确路径检查未发现附加文件。未进行全仓规则扫描。
- 设计与联合收尾阶段为 Phase 4；当时允许写入本文件、`p4-migration-note.md`及由周润基唯一编辑的`p4-design-deltas-v81.md`，H文件/Handoff/STATUS保持只读。后续六份文档归档与Git发布授权见§10.10。
- 设计阶段禁止写入业务正文、Contract、产品、数据库/migration/infra、P4-H文件、Handoff、STATUS、历史设计及未列入允许范围的文件，未执行Git写操作。发布阶段仅按§10.10新增六份文档归档和PR权限，其余禁止范围保持。
- 完成标准：A 有伪代码、单调性、AT-09～14 正反例及约束；C/E 有差异、约束和指定 AT 映射；G2 不少于半页；结论可追溯、PENDING 显式登记、概念不重复定义。AI 草稿不因核对通过自动变为正式交付。

**Z-AUTH。** 负责人 2026-09-06 的 Phase 3 豁免按周润基转述登记：忽略 Phase 3 Handoff 前置要求，允许直接开展本批次；无 Impact Matrix，改用 BD/AT/已提供的业务与 Phase 2 交付内容追溯。REQ 级追溯、独立 Phase 2 交付清单与本批次的正式对应关系留待 Phase 3 补做/联合收尾核实，不虚构 REQ 或交付编号。该授权不消除 PENDING、Contract 阻塞和 Phase 5 门禁。〔来源：本任务输入“upstream_handoff / impact_matrix”；本会话继续授权。〕

**Z-CONTRACT。** `1.2.0-contract / RC`；metadata 登记 OpenAPI SHA-256 为 `667ae751f3e623e3d603db4d68e6e9314d4b3fd6da433a1def8c36b81597d74a`。本轮仅核对 metadata 和 operation-catalog 的相关操作定位，不重新通读 OpenAPI，不重新执行 Contract verify 或历史客户端测试。旧测试数字不作为本轮结果。〔来源：[metadata](../../../contracts/contract-metadata.json)；[STATUS](../../rebuild/STATUS.md)“Contract 当前基线与阻塞、交接与证据边界”。〕

## 1. 来源索引与概念所有权

### 1.1 来源索引（各结论的〔S-*〕展开到此）

- **S-A**：BD-20260903-08；AT-09～14；[总览](../../business/00-overview.md) §4/8/11.1～11.3/14/15；[学生流程](../../business/10-student-flow.md) §7.1～7.4/7.8/8.1～8.2/9.4；[教师流程](../../business/20-teacher-flow.md) §7/10/12.1；[旧领域/数据库设计](../phase-3-domain-and-database-design.md) §5.3/5.4/5.6/5.7/6/7.5/7.6/7.8/8；[模块边界](../backend-module-boundaries.md) §2.1/4/6。
- **S-R**：BD-20260903-08/10；AT-22（本轮附加规则发布映射）；总览 §8；教师流程 §5.1～5.2；旧设计 §5.3/6/7.4/8；模块边界 §2.1 的 `course-enrollment` Owner。
- **S-C**：BD-20260903-06/07、BD-20260904-01/02；AT-19～21，补证边界另引用 AT-05～08；总览 §11.4～11.5/12.2～12.3；学生流程 §7.5～7.7；教师流程 §9.3～9.4/10.3；旧设计 §5.4/5.6/6/7.5/7.8/8；模块边界 §2.1/4。
- **S-E**：BD-20260903-04/09、BD-20260904-02；AT-16、23～25；总览 §7.1/8/9.4/21；学生流程 §6.1～6.3/8.3；教师流程 §5.3/6.1/7.2/12.2～12.3；旧设计 §5.3/5.7/7.2/7.4/8；模块边界 §2.1/4/5。
- **S-BF**：BD-20260903-01/06、BD-20260904-01/02；总览 §12/18；[公开原因交接](../../rebuild/handoffs/2026-09-04-review-public-reasons.md) §2；[复核决定交接](../../rebuild/handoffs/2026-09-04-business-review-followup.md) §2/4。审核、有效候选、计时与维护设计的正式位置归 P4-H。
- **S-AT**：[2026-09-04 业务方案交付](../../rebuild/handoffs/2026-09-04-teacher-first-business-update.md) §3/6/7 提供 BD 对照、工程影响与 AT。其 v8.0 的接管、成绩备注等冲突内容以 V8.1 正文及 BD-20260904-02 为准，AT-26/27 已作废，不恢复。
- **S-P**：复核决定交接 §4 的 P-20260904-01～04，以及 STATUS“前次删除指定待决说明”。待决正文小节删除不等于问题已解决。本稿只登记未决边界，不通过默认值补业务。
- **S-G2**：[迁移说明](p4-migration-note.md) §1～6；旧设计 §5.6“Existing data 与未来 migration 安全”；STATUS“Domain / 数据库、Backend 实现”。
- **S-H**：定向评审采用周润基提供的P4-H修订附件（历史身份如下；[当前正式稿](p4-design-huang-v81.md)提供同名定义导航），`p4-design-huang-v81(2).md`，982行、85532字节，SHA-256 `b4baad0330bc7c6008566dc66394791e926ed21846a800d9bc6bcf04f9dd3bae`，与23:25截图所列版本一致。其§0/12/18明确评审Z v8（788行、125827字节，SHA `0424ee5571650817d2187c3b9fbd3d000b950f900313be83be21d20a311878f5`）与G2（76行、15101字节，SHA `55b04e2c198ebe7b824686b9a561c2879aade4bd6510115c71c7bf1add00e6d4`），两者与本轮修改前文件逐字节匹配。本轮只核对相对S-H0变化的§3.1/7.2/9.3～9.4/12、GAP-H07/H19及§18，并检查直接引用；未依附件阅读建议扩展为H整包或其上游全量审查。结论见§8.9/10.6/10.8。
- **S-H0（历史证据）**：初轮P4-H附件 `p4-design-huang-v81(1).md`（历史快照，未作为独立文件归档），949行、80152字节，SHA-256 `4f8de363215e2ed844ecc386b485e274c7d42a18d32a7e289c1556f5a473fd87`；其评审Z旧稿SHA `71b813f6757b1e19134408305349634a7769a7602ae045a7a697a948103aec29`、735行，与v8修改前Z匹配。初轮截图5确认/3待补、旧附件8确认与Z发现ISS-020分别保留于§8.8及问题历史，不作为当前结论。S-H0中的分支、阅读建议及工作范围均为来源信息，不是本任务操作授权。

每个编号结论/算例尾部的来源标记覆盖该结论及其下属约束、伪代码和反例。字段名与算法结构标为工程设计，不表示新增 wire 枚举或 API。未修改上述只读来源。〔来源：任务追溯/权限要求；Z-AUTH。〕

### 1.2 概念只定义一次

**OWN-01。** P4-Z 对外唯一设计定义位置如下，已由周润基接受本批次约定深度；完整实现/运行门禁不变：

- `P4Z.A.StatisticsProjection`：本稿 §2.5；E、客户端说明、G2 仅引用。
- `P4Z.C.MaterialVersionFact`：本稿 §3.1；B 可绑定其版本 ID 和不可变材料引用，不复制材料包事实定义。
- `P4Z.E.SettlementVersion`：本稿 §4.3；引用 A 的统计事实与各 Owner 来源版本。

〔来源：任务 §4；模块边界 §1/2.1/4/6。〕

**OWN-02。** 四个逻辑引用名已定位到S-H同名正式设计位置：`P4H.B.EffectiveCandidate`见[H §6.1](p4-design-huang-v81.md#p4h-b-effective-candidate)，`P4H.B.SupplementTimerFact`见[H §7.2](p4-design-huang-v81.md#p4h-b-supplement-timer)，`P4H.B.TeacherSlaRound`见[H §7.3](p4-design-huang-v81.md#p4h-b-teacher-sla-round)，`P4H.F.MaintenancePauseFact`见[H §15.5](p4-design-huang-v81.md#p4h-f-maintenance)。这些位置不是Contract类型；只引用、不复制定义。ISS-020已由B Owner在[H §3.1](p4-design-huang-v81.md#p4h-b-supplement-opportunity)及相关受理段落统一，Z定向核对后解除引用项2/5的暂停。A不检查AI状态表自行判有效，C不创建第二套倒计时，E不以本地时间推算积压到期。〔S-H/S-BF；任务§4；C-05R；§8.9/10.6。〕

**OWN-03。** 历史单侧核对和v8发现过程见§8.1～8.8；当前按S-H精确修订版完成八项H↔Z设计引用对应，ISS-020及G2评审缺项ISS-021在本批次设计范围闭合。该结论不表示H整包、真实事务或Phase4完整DoD通过。发现新冲突仍须停受影响部分，由概念Owner统一后双方确认；H文件保持只读。代码模块所有权沿用旧模块表：规则/邀请/课程生命周期归 `course-enrollment`，会话时长归 `exercise-session`，媒体资产生命周期归 `media-evidence`，记录和材料包业务事实归 `exercise-record`，审核归 `record-review`，认证归 `applications-certification`，计算归 `statistics`；不因H附件中的组合投影来源标签新建模块。〔模块边界§1/2.1/4/6；任务§2/4；S-H；ISS-001/020/021。〕

## 2. A · 计入引擎与课程规则版本（深入级）

### 2.1 课程规则快照与发布校验

**A-01。** `course_rule_version` 为 `course-enrollment` 所有的拟议不可变规则快照：包含课程/学期、模板版本引用、规则版本号、两类目标、门槛、单次上限、日/周上限、上海周定义、允许日程、常规截止、七天收尾安排、预计结算时间、发布人与服务端发布时间。两类目标是非负整数分钟且合计 1200；门槛默认 30、闭集 30/45/60；单次上限 60、每日最多一条计入、周上限默认 3 且闭集 2/3/4。不存在第三类学时目标；游泳是项目要求。〔S-R；S-A；旧设计 §5.3/5.4。工程结构名，非 DDL。〕

**A-02。** 发布前配置为草稿，允许选择现行模板并调整许可参数。发布事务校验完整参数和可完成性，成功后固定规则版本；Session/Record 保存本课程发布版本引用，补证继续该引用。发布后拒绝修改目标、门槛、频次、公式和已公布日程；名称/说明仍依原规则编辑。模板更新不重定向已发布课程，不用 `current_target_revision_id` 任意换目标后追算历史。`course_target_revision` 原有历史保留要求继续，但不能成为发布后改目标入口。〔S-R；旧设计 §5.3/7.4。〕

**A-03。** 发布前可完成性要有可复核见证，不能仅验证 `目标合计=1200`：

1. 校验允许日程位于合法学期范围，常规截止、七天收尾和结算计划顺序符合正文；使用服务器上海日期/周。
2. 以允许日程产生潜在真实会话时段和业务日期容量，只有存在达到门槛的 ACTIVE 时段才有可计容量，单次至多 60；不能把不足门槛的短窗口当 60 分钟。
3. 将每日期的可实现类别候选与日/周约束一起做精确可达性计算，认证输入为 0；同时验证所选会话时段不重叠、未超出允许日程。输出达到两类目标的会话日程见证，才有充分的发布依据。
4. `60 × Σ每周 min(周上限, 可用日期数)` 只能作总容量必要条件，不是充分条件；还须校验分类容量、可用时段和学期范围。全员可完成性不能依赖尚未指定对象的补练或将来可能认证；已公布收尾期的个别补练不当作全员既得容量。
5. 工程上可复用 §2.3 的类别可达状态；潜在会话生成与时间相容性必须另外提供证据，不能用随机伪记录证明课程可完成。§2.10已给出有限目录的时间相容性参考算法；真实许可日程到完整目录的生成仍未实现，未宣称已验证真实课程计划。

反例：两类目标 610/590、每个可用日期最多 60、总共仅 20 个可计日期；总容量恰为 1200，但两类至少需 11+10=21 个日期，因此拒绝发布。正例：有足够长且互不重叠的 21 个允许日期、跨周满足 K=3、对应分配 11/10 条可达；仍要核对实际学期日程后才可发布。〔S-R；AT-22；总览 §8；教师 §5.2；旧设计 §5.3/7.4。〕

### 2.2 形式化目标与输入

**A-04。** 对原会话 ACTIVE 秒数 `s_i`，`m_i=floor(s_i/60)` 是实际整分钟；门槛为 H，`q_i=0 if m_i<H else min(m_i,60)` 是可计分钟。PAUSED 不参与累计；若旧模型以毫秒保存，先使用同一完整 ACTIVE 总量换算，等价于 `floor(actual_duration_ms/60000)`，不能逐区间先取整再求和。实际秒/毫秒保持原事实，不能用 q 回写。业务日期是服务器接受开始时的上海日期，周为该日期所属周一至周日，补证和跨午夜均不变。〔S-A；AT-09/11；旧设计 §5.4。〕

**A-05。** A 消费一个一致输入快照：Enrollment/课程范围、锁定规则版本、P4-H 的 `P4H.B.EffectiveCandidate` 结果及其来源版本、候选对应 Record/Session 的不可变类别/日期/开始 instant/稳定 ID/ACTIVE 秒、`applications-certification` 当前有效认证汇总、上一次计入组合及其输入版本。A 不定义有效候选判定。候选缺失、不一致或 B 状态未知时不得用提交成功或旧默认 VALID 补足；保持统计不可用/待重算，不回传假零进度。〔S-A；S-BF；模块边界 §2.1/4/6。〕

**A-06。** 设类别 c∈{课程相关,其他}，目标 T_c，当前有效认证汇总 C_c，选择集合 S。可行集合满足：来自 B 有效候选、同日期至多一条、同原业务周至多 K 条；每条 q≤60。优化目标为 `F(S)=Σ_c min(T_c, C_c+Σ_{i∈S,category=c} q_i)`，`T_0+T_1=1200`。日/周名额只约束运动记录，不约束认证；一类超额不转另一类；课程级不得合班后统一封顶。〔S-A；AT-10/11/14。〕

**A-07（本会话已确认的来源分配口径）。** 周润基在本会话确认：认证先占分类额度，再按选中记录的运动开始 instant、稳定 ID 分配剩余额度。因此 `b_c=min(T_c,C_c)`、`R_c=T_c-b_c`；逐条 `a_i=min(q_i,尚余R_c)`，未选中记录 a=0。例如 T=600、C=570、q=60，分类完成 600，认证实际贡献 570、该记录实际计入 30。该确认解决逐条归属，不改变 A-06 的最优总量，不新增 BD 编号，不修改业务正文或 Contract。认证的批准/撤销、分类及多认证汇总仍由原 Owner 提供。〔S-A；总览 §11.3/14；本会话对“接受上述分配口径”的明确答复。〕

### 2.3 精确选择伪代码（设计核对载体）

**A-08。** 以下 Python 风格伪代码在内存算例中可执行，用于核对设计，不是 Backend 实现或数据库 migration。输入均已通过各事实 Owner 校验；`started_at` 比较的是服务端 instant，不是任意时区字符串；`day` 是固化上海日期。分类索引 0/1 是本伪代码局部记号，不是新 wire 枚举。〔S-A；A-04～07；旧设计 §5.4/5.7；模块边界 §6。〕

```python
from collections import Counter, defaultdict
from datetime import timedelta

def eligible_minutes(active_seconds, threshold):
    whole = active_seconds // 60
    return 0 if whole < threshold else min(whole, 60)

def business_week(day):
    return day - timedelta(days=day.weekday())

def select_credit(candidates, targets, certification, threshold, weekly_limit,
                  previous_ids=()):
    # candidates 只接收 P4-H「B-有效候选结果」；此处不判定审核有效性。
    # started_at 是可比较的服务端 instant，day 是固化的上海业务日期。
    assert threshold in (30, 45, 60) and weekly_limit in (2, 3, 4)
    assert len(targets) == len(certification) == 2
    assert all(isinstance(t, int) and t >= 0 for t in targets)
    assert sum(targets) == 1200 and all(c >= 0 for c in certification)
    assert len({r["id"] for r in candidates}) == len(candidates)
    base = tuple(min(targets[c], certification[c]) for c in (0, 1))
    room = tuple(targets[c] - base[c] for c in (0, 1))
    rows = sorted(
        [dict(r, q=eligible_minutes(r["active_seconds"], threshold))
         for r in candidates],
        key=lambda r: (r["started_at"], r["id"]))
    positive = [r for r in rows if r["q"] > 0]
    by_id = {r["id"]: r for r in positive}
    bit = {r["id"]: 1 << (len(positive) - 1 - i)
           for i, r in enumerate(positive)}
    by_day = defaultdict(list)
    for r in positive:
        by_day[r["day"]].append(r)

    # key=(本周已选条数, 两类已分配运动分钟); value=(平局位序, 见证ID元组)
    dp = {(0, 0, 0): (0, ())}
    last_week = None

    def keep(table, key, witness):
        if key not in table or witness[0] > table[key][0]:
            table[key] = witness

    for day in sorted(by_day):
        week = business_week(day)
        if week != last_week:
            reset = {}
            for (_, x, y), witness in dp.items():
                keep(reset, (0, x, y), witness)
            dp, last_week = reset, week
        nxt = {}
        for (k, x, y), (mask, ids) in dp.items():
            keep(nxt, (k, x, y), (mask, ids))  # 当天不选
            if k == weekly_limit:
                continue
            for r in by_day[day]:            # 当天至多选一条
                amounts = [x, y]
                c = r["category"]            # 0=课程相关，1=其他
                amounts[c] = min(room[c], amounts[c] + r["q"])
                if amounts == [x, y]:
                    continue                # 实际贡献0，不占计入名额
                key = (k + 1, amounts[0], amounts[1])
                keep(nxt, key, (mask | bit[r["id"]], ids + (r["id"],)))
        dp = nxt

    (best_key, best_witness) = max(
        dp.items(), key=lambda item: (item[0][1] + item[0][2], item[1][0]))
    optimum = sum(base) + best_key[1] + best_key[2]
    chosen = best_witness[1]

    def allocate(ids):
        remaining = list(room)
        actual = {}
        for r in sorted((by_id[i] for i in ids),
                        key=lambda r: (r["started_at"], r["id"])):
            c = r["category"]
            actual[r["id"]] = min(r["q"], remaining[c])
            remaining[c] -= actual[r["id"]]
        totals = tuple(targets[c] - remaining[c] for c in (0, 1))
        return actual, totals

    # “保留原组合”按完整集合判断，不自行增加“尽量保留交集”规则。
    old = tuple(previous_ids)
    if len(set(old)) == len(old) and all(i in by_id for i in old):
        old_rows = [by_id[i] for i in old]
        unique_days = len({r["day"] for r in old_rows}) == len(old_rows)
        counts = Counter(business_week(r["day"]) for r in old_rows)
        if unique_days and all(n <= weekly_limit for n in counts.values()):
            old_actual, old_totals = allocate(old)
            if all(v > 0 for v in old_actual.values()) and sum(old_totals) == optimum:
                chosen = tuple(r["id"] for r in sorted(
                    old_rows, key=lambda r: (r["started_at"], r["id"])))
    actual, totals = allocate(chosen)
    assert sum(totals) == optimum
    return {
        "selected_ids": chosen,
        "certification_counted": base,
        "category_totals": totals,
        "total": optimum,
        "record_actual": {r["id"]: actual.get(r["id"], 0) for r in rows},
    }
```

### 2.4 正确性、平局与规模边界

**A-09。** 动态规划状态 `(k,x,y)` 完整保留本周已占名额及两类已分配运动分钟；每个日期只能不选或选择当天一条，换周只清零 k、保留两类累计。分类累计饱和到 R 后，多余分钟不影响未来可达结果。同一状态的后续选择集合相同，因此不能因为某一类当前更多就丢弃另一类别状态，也不使用每周单独求一个最高标量的贪心。〔S-A；A-06～08。〕

**A-10。** 总量优先于所有平局偏好。先求最大 F，再检查原完整组合是否仍来自当前有效候选、日/周合法、各条有实际贡献且 F 等于最优值；成立则保留原组合。原组合已非最优或不再合法时，按候选 `(开始 instant,稳定 ID)` 排序，最早出现差异的位置优先保留该记录；实现为较早候选对应较高位、1 优先于 0 的成员位序。该位序只用于同一状态或同总量比较，追加共同的未来选择不改变已有位序优先级。没有另加“最大化原组合交集”或“最少记录条数”规则。无正贡献的候选不占计入名额；其运动记录仍保存。〔S-A；总览 §11.3；A-07～08。〕

**A-11。** 单调性证明：规则、目标、认证及统计对象范围不变，新增有效候选得到集合 E'⊇E；旧最优组合仍是 E' 中的可行组合，故 `max F(E')≥max F(E)`。补证通过只扩充 E，不改变旧记录日期/周、q 或规则，所以同样成立。DP 不会因较早补证通过强制替换更优记录；保留原组合只是等总量时的偏好。纠正无效、撤销认证或成员范围变化不满足此前提，可能使结果减少，必须展示来源变更原因。每条记录的 a 不要求单调，总有效进度才是此处保证对象。〔S-A；AT-13/14。〕

**A-12。** 完备性说明：任何合法组合都可按业务日期依次作“不选/选一条”的转移；若某条转移增加 0，删掉它不会改变后续分类完成量，且只释放名额，因此跳过零贡献转移不会损失最大值。相同 `(k,x,y)` 保留位序较优的见证仍覆盖全部可能的未来结果。空候选集也有 `(0,0,0)`，结果是认证封顶量，不伪造运动。类别目标为 0 时该类记录仍保存但不计入。〔S-A；A-06～10。〕

**A-13。** 分钟整数状态上限为 `(K+1)(R_0+1)(R_1+1)`，R 总和不超过 1200，最宽类别乘积为 601×601。时间量级为 `O((D+N)·K·(R_0+1)·(R_1+1))` 的状态转移，D 为有候选日期数、N 为候选数；此量级不包含 Python 见证元组复制、大整数位序和回溯存储成本。伪代码优先可审查性，生产实现还需压缩可达状态、父指针重建和真实规模评估。不能为了性能改为未经证明的贪心；未执行性能测试，不将此参考算法宣称为已满足生产容量。〔工程分析；A-08；S-A。〕

### 2.5 P4Z.A.StatisticsProjection（唯一统计定义）

**A-14。** Record 投影保留原始 ACTIVE 秒及实际整分钟 m，并分列：可计分钟 q、实际计入分钟 a、未计入原因。`0≤a≤q≤60`；q=0 或 B 不提供该记录为有效候选时 a=0。可计量只描述时长公式潜力，不等于审核通过。显示 B 的原审核/流程状态，不能把待处理或有效未选中改写为 INVALID；无效原因由 B 提供，A 不扩展六类公开审核原因。〔S-A；S-BF；总览 §11.3/12.1/15。〕

**A-15。** 未计入原因是可复核的计算解释，不是新的审核分类或 Contract enum。按事实列出：未达门槛；超过单次 60 的实际分钟；本类剩余额度不足（可部分计入）；同日已有实际计入记录；原业务周计入名额已满；联合最优组合未选中。多个原因可同时成立，不把联合约束硬说成唯一原因；只有真实达到相应上限才显示具体日/周原因，并可引用选中记录 ID/原周和规则版本。B 的未完成审核/无效原因单独引用。〔S-A；AT-09～14；A-06/14。工程解释字段，非新增业务目录。〕

**A-15A（解释对应的分钟范围）。** A-15的解释须区分时长公式和选择结果：`m-q`解释未达门槛或单次封顶，`q-a`解释有效候选的可计量未全部计入，两者不得混成A-17的有效未计入列。有效90分钟且q=a=60时，仅有公式未纳入30，不得说60可计分钟未计入；有效29:59时q=a=0、U=0，解释门槛即可，不能把该29分钟归因为日/周名额竞争。已取得的无效/待处理结果只引用B；B来源未知时沿A-05保持待取得一致结果，不能由A猜成某种审核状态。〔S-A/S-BF；总览§11.3/15/21；AT-09/12/14；A-04/14/15/17；本会话第二项统计确认；旧设计§5.4/5.7。〕

**A-15B（当前组合中的限制事实）。** 对B已明确有效且q>a的记录，解释使用同一个已核对投影：若a>0，剩余q-a只能来自本类目标截断，不能因为该记录自己占用了名额而解释为“同日已有记录”或“原周已满”。若a=0且q>0，列明当前组合实际存在的本类已满、同日其他实际计入记录及原业务周名额已满，引用对应选择版本/记录/原周；日/周计数仅统计a>0的运动记录，不统计认证、待处理、q=0或其他未选中记录。多个事实可并存，但只是对当前组合的解释，不表示该记录违规、不表示只要取消某一限制就一定入选，也不意味着所有替换方案都无法计入；“联合最优组合未选中”继续指向A-08/10的完整选择。此处不指定客户端布局或新wire枚举。〔S-A；总览§11.3；AT-10～14；A-06～10/15/17；旧设计§5.4/5.7；ISS-018。〕

**A-15C（解释不能掩盖错误结果）。** 在A-06固定输入与合法候选前提下：①若0<a<q而本类完成量p<T，说明分配与封顶不一致；②若a=0、q>0、本类未满，且该日无实际计入记录、原周仍有名额，则直接加该记录就会提高总量，原结果并非最优。两者都应停止此次投影发布并重新核对计算/来源，不追加虚假的“联合限制”或把运动改判无效。这些是充分的异常判据，不是完整最优性证明；其余结果仍必须经过A-08的精确选择与A-22的来源保护，不能以“已生成原因”代替正确性验证。〔A-06/08/14～17/20/22；S-A；AT-10/11/13；总览§11.3；旧设计§5.7；ISS-018。〕

**A-15D（本轮解释核对）。** 在内存中用未改动的A-08结果核对8种解释情形：90分钟全部可计量已计入；认证后部分计入；部分计入且本周名额正好满；29:59且同日/周名额都满；同日30被60替代；同周2名额取两条60而不取30；认证独自填满目标；联合分类选择同时出现本类/同日/周限制。8项均与A-15A/B的分列一致，其中部分计入虽处于满额周也只解释本类截断，认证填满不产生日/周占用。另注入A-15C两种内部不一致，均被对应异常判据拒绝。合计10种情形均得到预期核对结果；拒绝指内存一致性检查，不是数据库事务已执行。未运行B判定或客户端展示，也未重跑之前的随机选择样本。〔本轮10种内存情形输出；A-15A～15C；S-A；AT-09～14；ISS-018。〕

**A-16。** 学生分类结果分列认证当前认可汇总 C、封顶后的认证贡献 b、选中运动实际贡献 Σa、完成量 `p=b+Σa=min(T,C+Σ选中q)`、剩余 `T-p`；总量 `p_0+p_1≤1200`。认证超额不转类、不占日/周名额，不把可计记录总和直接塞入完成量。达标用原始整数分钟 1200；百分比只做展示四舍五入，例如 1199/1200 显示 100% 仍未达标。学生不得看到分数/等级/排名；进度不用于自动填最终成绩。〔S-A；AT-12/14；总览 §11.3/15；旧设计 §5.7。〕

**A-17（本会话已确认的报表口径）。** 周润基明确选择“仅统计可计分钟减实际计入”：有效未计入 `U=Σ当前有效记录(q_i-a_i)`。实际无效运动分钟 `I=Σ当前审核无效记录m_i`，必须用原实际整分钟，不用 q 或进度减少量替代；审核有效集合和无效集合由B结果提供，待AI/待教师/待补证/技术处理中单列待处理，不归入 I 或 U。例如有效90分钟、q=60、a=60时 U=0；公式未纳入的30分钟另以 `m-q` 解释，不混入 U。有效记录实际整分钟可核对为 `Σa+U+Σ(m-q)`，认证贡献单独列示，不加入实际运动秒数。无效90分钟记录I=90，即使其q仅60；更正无效后实际进度减少多少仍由完整重算决定。该列只在A定义，E引用本条。〔S-A/S-E；总览 §11.3/21；教师 §12.2；本会话对“仅统计可计分钟减实际计入”的明确答复。〕

**A-18。** 课程汇总以 `course-enrollment` 提供的成员范围与来源版本为准，先逐学生执行选择、分类封顶，再求总量与人均。当前有效成员口径与历史/结算名单口径分开标明，分母来自同一范围；未确认分母/正式完成学生人数时不猜完成率。学生移出后不把旧班进度搬到新班、不删旧事实；当前汇总可按 Owner 新范围排除该学生，历史报告仍保留原成员快照。〔S-A；AT-14；总览 §7.3/9.3/11.3；教师 §7；旧设计 §5.3/5.7。〕

**A-19。** 投影输入令牌至少关联规则、Record 事实集合、B 有效候选版本、认证版本、成员范围版本及前驱选择版本；重复来源事件不重复计入。输出同时包含算法策略版本、选中集合、三分列及两类/总量，不能拼接不同读取时点的数据。原组合偏好需要持久的可重建选择检查点，不能只存在易失缓存：检查点与前驱引用由 `statistics` 管理，禁止客户端写，也不是新的运动/审核事实。缓存丢失时从检查点或有序来源版本重建，不能随意当作首次计算。〔S-A；旧设计 §5.7/7.1；模块边界 §4.2/4.3/6；A-05/10。工程一致性落点。〕

### 2.6 关键约束、事务与索引增量

**A-20。** 数据约束草案：

- `UNIQUE(session_id)` 继续保护一会话一记录；同一学生最多一条 ACTIVE/PAUSED 会话、开始业务日期和正式时长不可变继续沿用 Session Owner 约束。
- 删除设计中 Record 的 `UNIQUE(enrollment_id,business_date)`；保留多条独立真实运动。同日/周唯一性只校验当前计入选择：同一投影版本同日实际计入最多一条、同周实际计入条数≤K，不能约束所有 Record 历史。
- 规则快照目标非负、合计1200、H∈{30,45,60}、K∈{2,3,4}；发布引用只能指向本课程版本且锁定。Record 的规则引用、类别、日期、时长保持不可变，q 按 A-04 精确推导；废止 `credited_minutes CHECK IN (0,60,120)` 及旧映射。
- 实际计入 a 位于派生投影，约束 `0≤a≤q`；分类完成量不得超过目标、总量≤1200；跨行日/周/分类约束通过同一输入版本的完整选择核验，不能伪装成一个普通单行 CHECK 就已足够。
- 任一物化选择的 Record 必须属于同一组织/课程/Enrollment 和规则版本；组合、逐条明细、分类合计与投影指针一起发布，禁止只有合计更新、明细仍旧。

〔S-A；旧设计 §5.3/5.4/5.7/6/8。均为未来结构要求，未执行 DDL。〕

**A-21。** 发布规则事务沿用 `course-enrollment` 的版本校验与原子提交；Session 开始不再调用“进度<1200”或“日/周未满”作为准入条件。保留身份、邮箱、成员、学期、课程、时间、模式和唯一进行中会话的原检查；补练资格引用 E。新增候选、补证判定、认证变更通过各 Owner 公开能力触发重算，A 不直接写 Review、认证或读取跨模块私有表。〔S-A；S-E；旧设计 §7.4/7.5/7.6/7.8；模块边界 §4/5/6。〕

**A-22。** 重算可在短事务外完成纯计算，但发布检查点时必须校验来源令牌和前驱投影版本未变，变化则丢弃过期结果并重算；慢任务不能把旧结果覆盖新认证/审核状态。正式结果展示应标明来源一致版本；重算未完成不冒充新事实已反映。结算冻结时需与 E 的阻塞检查和来源版本复核协作。继续沿用命令幂等、expected version、同事务审计/必要通知原则，具体事件载荷与并发实现留后续 Phase 4。〔S-A/S-E；旧设计 §7.1；模块边界 §4；A-19。〕

**A-23。** 索引只提出查询目的，不执行/承诺 EXPLAIN 结果：候选读取用 `(enrollment_id,rule_version_id,business_date,started_at,id)` 非唯一索引；可重建明细用 `(selection_revision_id,record_id)` 唯一关联，日/周实际计入索引用于检查；规则版本保留 `(course_id,version_no)` 唯一和 command 幂等。旧 Daily Record 唯一索引废止，旧按 `credited_minutes` 汇总索引不能证明新选择有效。是否物化、索引列顺序与成本须用后续真实数据核定。〔S-A/S-R；旧设计 §8；模块边界 §4.3/6。〕

**A-22A（续作Review：来源集合与原子发布）。** A-22的“检查后发布”必须落实为同一受保护发布点，不能只是两次普通读取。输入令牌必须覆盖候选集合、认证集合、成员范围以及各事实版本；只记录已读行的版本不能发现新加入的有效候选。集合一致性标识由相应Owner公开读取能力提供，A不得通过直查B私有表自行拼造。B侧来源保护已对应[H §6.2](p4-design-huang-v81.md#p4h-b-source-guard)，A只引用其公开能力；认证、成员等其他Owner的对应能力仍须分别确认，不要求B代供全部事实，不在此定义B候选判定。〔S-A/S-BF/S-H；A-05/19/22；旧设计§7.1；模块边界§4.1/4.3/6；ISS-001/012。〕

**A-22B（续作Review：发布步骤）。** 拟议的内部 `TryPublishSelection` 不是Contract接口：计算前取得一致来源快照与前驱选择版本P，纯计算在事务外进行；发布时通过Owner参与式公开能力取得来源版本保护，按既有统一锁顺序在同一Unit of Work内复核集合/行版本及P，再原子保存完整明细、合计和新选择指针。来源Owner的集合增删及相关mutation必须遵守同一保护协议，保护持续到提交，不能在复核与CAS之间留下允许来源变更的空隙；若无法取得这种保证，则不能将结果发布为当前。任何来源令牌或前驱P变化都舍弃该次发布并重读/重算，不能仅把旧计算结果换上新版本号。重复提交相同来源、前驱和策略版本的完整结果可返回既有检查点，但须携带原来源版本；幂等返回不表示该旧来源仍是当前，不回切当前指针，来源已变时另行重算。来源业务事实不因重算排队失败回滚成假状态。〔S-A；A-19/22；旧设计§7.1；模块边界§4；ISS-012。工程设计，真实锁/事务NOT EXECUTED。〕

**A-22C（续作Review：三组并发反例）。** ①任务读取候选集合v7后新候选到达成为v8，即使旧Record行都未变，v7结果也不能发布为v8；因此需要集合标识。②任务复核v7后释放保护，另一事务写v8，再写选择指针，会产生检查与写入间隙；A-22B要求保护覆盖到提交以排除此顺序。③两个任务都读取前驱P3，第一任务发布P4后，第二任务不得覆盖；仅当确认请求与完整结果已对应同一个已发布检查点时幂等返回，否则基于当前前驱重新计算。这些是顺序推演，不是数据库并发实测。〔S-A；A-10/19/22A/22B；旧设计§7.1；ISS-012。〕

**A-19A（续作Review：原组合与恢复）。** 同一候选/认证快照可能有多个最优组合，原组合偏好包含历史上下文：只凭最新候选重新排序不能保证恢复原展示。因此重放需同一前驱选择及一致输入；缓存丢失读取持久检查点，必要时从有序输入与前驱链重建，不能把空缓存当作原组合为空。若检查点和可重放来源均缺失，显式保留待恢复，不宣称已经恢复同一选择。G2恢复边界只引用此条件；不得为修复展示重新改审核或认证事实。〔S-A/S-G2；A-10/19；G2-10/12/15；ISS-012。〕

### 2.7 AT-09～14 正反例逐步推演

以下假设会话合法、无重叠，日期由服务器固定；记录是否进入候选只消费 B 结果。除特别标明，H=30、K=3、目标600/600、认证0、同类目标仍有足够余额。算术核对不执行业务审核或产品接口。〔S-A；S-AT；A-04～08。〕

**A-AT09 / AT-09。** 对 ACTIVE 29:59、30:00、45:59、90:00，s 分别为1799、1800、2759、5400，m 为29、30、45、90；先判门槛后 q 为0、30、45、60。无其他限制且为有效候选时 a 同 q；原时长仍是29:59/30:00/45:59/90:00。反例：将29:59进位为30、将30算60、将90算120、把PAUSED加进ACTIVE均不符合公式。H=45 时44:59→0、45:00/45:59→45；H=60 时59:59→0、60:00/90:00→60。所有 ACTIVE 区间先求和后取整，例如两段29.9秒加0.2秒共60秒为1整分钟，不能逐段取整为0（该例因门槛仍q=0）。〔S-A；总览 §11.2/11.3；旧设计 §5.4。〕

**A-AT10 / AT-10。** 同日两个独立会话10:00开始30分钟、12:00开始60分钟，均有效。DP 当天的可达状态包含不选0、选早记录30、选晚记录60，不能从选30状态再加同日60；最优为60，两条事实保留，前条 a=0、后条 a=60。反例：同日相加90违反每日上限；取最早30损失进度。联合分类反例：认证540/540，D1有课程相关60或其他59，D2只有课程相关60，K=2。D1取最长相关60后D2无额外贡献，总1140；D1取其他59、D2取相关60，得到分类600/599、总1199。因此不能先按每日最大值剪枝。〔S-A；AT-10；A-06/09。〕

**A-AT11 / AT-11。** 同一周四个日期的 q 为30、45、60、60。K=3 时逐日保留不同类别/条数状态，最终选45+60+60=165，第四条可以参与选择，不把它本身标无效；K=2 为120，K=4 为195。反例：默认三条却累计195，或只取最早三条得135。

跨午夜子例：2026-09-06周日23:45开始、周一00:45结束仍属9月6日与8月31日开始的周。该原周已有三条60，9月7日又有一条60，则前周最多180、后周60，总240；按结束日把周日记录搬到后周可能错误得到300。

跨周补证子例：9月6日30分钟记录在9月8日补证通过，仍加入原周候选。原周已有三条60、9月7日新记录60时，总量仍240，补证记录不挤掉60，也不占9月8日/该周名额。先前仅待补证时 B 不把它交给 A；通过后才参加同一次精确重算。〔S-A；AT-11/13；总览 §11.2/11.3；旧设计 §5.4。〕

**A-AT12 / AT-12。** 已达到1200后，仍处于合法课程时间的学生可开启独立新运动；不能用进度门禁拒绝。设认证已覆盖600/600，新90分钟有效记录 m=90、q=60，剩余R=0，DP跳过零贡献选择，a=0，总量仍1200；原因是目标已满与单次可计上限，审核状态仍消费B的有效结果。以运动记录达标的情形同理：无新增总量时原组合优先保留。反例：不允许建会话、把有效未计入改为INVALID、或计到1260。〔S-A；AT-12；总览 §11.1/15；旧设计 §7.5 的冲突项。〕

**A-AT13 / AT-13。** 同日12:00的60分钟记录已计入，10:00的30分钟记录此前待补证；补证通过后加入候选。原组合F=60仍可行，新组合只有30，DP仍取60；不会因运动时间更早/补证晚审而降低进度。不同日期但同周名额已满时同理：原三条60=180不能被新增30强制替成150。平局子例：认证570/600，剩余相关30；原选较晚的30分钟，新来较早60分钟时两种方案都是1200，保留原完整组合；不存在可保留原组合时才按开始instant，再按稳定ID决定。〔S-A；AT-13；A-07/10/11。〕

**A-AT14 / AT-14。** 两类目标600/600，同周有三条相关60。认证原为0/500时结果为相关180、其他500，总680；新增认证至570/500后，相关只需30，按已确认归属第一条 a=30、余两条 a=0，总1100。撤销相关认证后重算恢复三条各60，总680；减少420而非直接扣570，因为记录释放出的150可重新计入。认证不可被当成日/周记录：认证300/300、K=2，两个不同日期的相关60和其他60都可选，总720。

成员与课程汇总子例：同班两名学生分别完成1200与600，课程累计1800、人均900；不能全班合计再封顶成1200。移出第一人后，若课程 Owner 当前有效成员集合只剩第二人，则当前口径累计600、人均600；原第一人的课程事实、旧报告及原汇总版本保留，新班不接收这些旧记录。反例：把移出当作删记录、把历史报告随当前成员改写、混用原分母2得到人均300。〔S-A；AT-14；总览 §9.3/11.3/14；教师 §7/10.3；旧设计 §5.3/5.6/5.7。〕

### 2.8 旧设计处置清单（不修改旧文件）

**A-24。** 下列设计由本增量替代，后续实现不得沿用：旧设计 §5.4/6/7.5 的0/60/120 CHECK与映射→A-04/20；§5.4/6/8 的 Record 日期唯一→投影日期名额；§5.7 的直接求和封顶 view→A-06～19；§7.5 的达1200禁止新会话→A-21；§5.3/7.4 的发布后切换目标→A-01～03。模块边界 §2.1 的旧阶梯描述与 §5/6 将 statistics eligibility 当作进度准入的旧用法仅登记失效点，保留各模块Owner及公开端口原则。〔S-A/S-R；AT-09～14/22。〕

**A-25。** 旧 §5.4/6/7.5 “提交默认VALID”、只靠VALID/INVALID表示所有流程的冲突交给P4-H B处理；A只引用新的有效候选结果，不在此修订Review状态机。旧 §5.7/7.9最终成绩备注不再适用，已登记Contract缺口由P4-H负责，本稿不改成绩模型、Contract或业务正文。〔S-BF；BD-20260904-02；总览 §12/15；任务范围。〕

### 2.9 续作新增的设计核对证据

**A-VERIFY-02。** 为补ISS-012首轮尚未覆盖的“完整选中组合是否稳定”证据，使用固定种子412013生成72组2～8条候选，含同日选择、跨周、0目标、剩余1/29/30/45/60/61等边界；按既有公式、日/周约束和已确认来源分配，独立枚举所有子集，直接用开始instant/稳定ID的成员优先序比较完整组合，并校验原完整组合优先。全部与A-08的选中ID、逐条实际计入、分类合计、总量一致。另完成216次输入重排、72次用输出组合作为原组合的同快照重算、54次从输入移除已选记录后的独立对照，均通过。移除候选只是模拟B输入变化，不执行或重新定义B审核。首轮120组样本未重跑，A-08代码未改变。〔S-A；AT-10/13/14；A-08～12/19A；本次Python内存设计核对输出。〕

**A-PUBLISH-VERIFY / AT-22。** 明确假设一课程的正式允许时段容纳以下互不重叠时段且位于合法学期：2026-09-07起连续7周，每周一/三/五09:00各一段60分钟，两类项目均可选，H=30、K=3、认证为0，目标610/590。该构造21个不同业务日期，精确选择11条课程相关、10条其他，分类完成610/590、总1200；每周正好3条。删除最后一个日期只剩20个时，同一算法最大1190（600/590），不能因总原始容量1200而发布。此为条件明确的设计算例，不代表该真实课程/学期存在、已建立潜在会话生成器或验证了全部允许日程。〔S-R；A-03；AT-22；本次内存计算输出；ISS-013。〕

**A-REPORT-VERIFY。** 按A-17核对四组报告分列：有效90分钟无认证→q60/a60/U0/公式未纳入30；有效90分钟且本类已有认证570→q60/a30/U30/公式未纳入30；有效29:59→实际整分钟29/q0/a0/U0/公式未纳入29；有效30分钟→q30/a30/U0/公式未纳入0。四组均满足实际整分钟＝运动实际计入＋有效未计入＋公式未纳入；认证贡献另外分列。结果仅核对已确认报表口径，不证明B会将任一真实记录判为有效。〔S-A/S-E；A-07/17；本次内存算术输出；ISS-009/010。〕


### 2.10 发布前计划目录的时间相容性补强

**A-PLAN-01。** A-08的真实候选已满足原会话合法性，不能把尚未发生、可能相互重叠的计划选项直接当作B有效候选调用它。发布校验应使用课程Owner确认合法的计划时段与规则依据；这里只验证给定选项间的计入与时间相容性，不决定新的结束期限，不把常规截止自行解释成所有会话必须结束的时刻，不放宽已确认的课程/学期条件。该目录只是证明用数据，不创建Session/Record，不在未来替代B审核。〔S-R/S-A；AT-22；总览§8/11.1/11.2；旧设计§5.3/5.4/7.5；A-03；ISS-013。〕

**A-PLAN-02。** 三种证据结论必须分开：找到达到1200的合法且互不重叠见证，可以证明存在可完成计划；没有找到见证，仅在输入目录覆盖全部许可选择或已有严格不可能性证明时，才能认定无法完成；目录不完整、合法性依据缺失或搜索未完成时，只能记证据不足并保持草稿。不能用有限样本失败、随意按整点/整分钟离散化或搜索超时，伪装成已证明课程不可完成。发布仍以已确认业务及充分证据为前提，不新增业务状态或Contract枚举。〔S-R；AT-22；总览§8；教师§5.2；A-03/ISS-013。〕

**A-PLAN-03。** 在有限、已确认合法的计划目录内，使用下列精确可达状态补充时间约束。状态比A-08多保存最近选中会话的结束instant；换周只清名额、不清结束instant。选项占用从开始到结束的完整会话时段，PAUSED期间也不能容纳另一会话；ACTIVE秒数只用于可计公式。不同日期并不保证互不重叠。此参考算法只需返回任一可行最优计划见证，真实记录的原组合优先仍仅由A-08定义。〔S-R/S-A；AT-11/22；旧设计§5.4；A-04/08；ISS-013。设计伪代码，非产品实现。〕

```python
from collections import defaultdict
from zoneinfo import ZoneInfo
from datetime import timedelta

def plan_catalog_max(options, targets, threshold, weekly_limit):
    # 前提：课程Owner已确认这些计划选项合法；不是B的真实有效记录。
    # 每项含option_id/start/end/active_seconds/category；不写Session/Record。
    assert threshold in (30, 45, 60) and weekly_limit in (2, 3, 4)
    assert len(targets) == 2 and sum(targets) == 1200
    assert all(isinstance(t, int) and t >= 0 for t in targets)
    assert len({o["option_id"] for o in options}) == len(options)
    assert all(o["category"] in (0, 1) for o in options)
    assert all(o["start"].utcoffset() is not None and o["end"].utcoffset() is not None for o in options)
    shanghai = ZoneInfo("Asia/Shanghai")
    groups = defaultdict(list)
    for option in sorted(options, key=lambda o: (o["start"], o["option_id"])):
        assert option["end"] > option["start"]
        assert 0 <= option["active_seconds"] <= (option["end"]-option["start"]).total_seconds()
        day = option["start"].astimezone(shanghai).date()
        groups[day].append(option)
    # key: 本周条数、两类完成量、最近已选会话的结束instant
    dp = {(0, 0, 0, None): ()}
    last_week = None
    for day in sorted(groups):
        week = day-timedelta(days=day.weekday())
        if week != last_week:
            reset = {}
            for (_, x, y, end), ids in dp.items():
                reset.setdefault((0, x, y, end), ids)
            dp, last_week = reset, week  # end不能随换周清空
        nxt = dict(dp)                  # 当天不选
        for (k, x, y, end), ids in dp.items():
            if k == weekly_limit:
                continue
            for o in groups[day]:
                if end is not None and o["start"] < end:
                    continue           # PAUSED也占据该会话的完整时段
                whole = o["active_seconds"]//60
                q = 0 if whole < threshold else min(whole, 60)
                amounts = [x, y]
                c = o["category"]
                amounts[c] = min(targets[c], amounts[c]+q)
                if amounts == [x, y]:
                    continue
                key = (k+1, amounts[0], amounts[1], o["end"])
                nxt.setdefault(key, ids+(o["option_id"],))
        dp = nxt
    state, ids = max(dp.items(), key=lambda item: item[0][1]+item[0][2])
    return {"catalog_max": state[1]+state[2],
            "category_totals": (state[1], state[2]), "witness": ids}
```

**A-PLAN-04。** 完备性仅针对所给有限目录：按上海业务日期遍历，每天不选/选一项覆盖日约束；所有前序选中会话若已互不重叠，则只需比较新开始是否早于最近结束。相同k、两类完成量和最近结束的后续可达选择相同，保留一份见证不丢失目录内最优值。跨周保留结束值避免把周日跨午夜会话与周一会话同时选入；略去零贡献选择不损失目标值。目录生成、时间离散化完整性和生产规模仍待后续工程证明，不因这段伪代码可执行就宣称通用发布求解器完成。〔A-PLAN-01～03；S-R/S-A；旧设计§5.4；ISS-013。〕

**A-PLAN-VERIFY。** 本轮使用固定种子422015，对48组0～8项目录作独立子集穷举，按完整会话占用区间、日/周和两类目标过滤，目录最优值均与参考算法一致；没有重跑此前的记录选择样本。另核对下列反例：

- 周日23:30开始60分钟、周一00:10开始60分钟：仅按日期/名额会计120，实际时段相容选择最多60；增加周一02:00开始的同日替代项后，找到周日＋02:00的120分钟见证，不能只拒绝最先得到的冲突方案便断言无解。
- 周日23:00至周一01:00的会话只有60分钟ACTIVE，中间存在PAUSED；另一条周一00:00至00:30落入该会话占用跨度，仍不能同时选。忽略占用跨度会误算90，正确最多60。前一会话结束与后一会话开始相接的样例可合计120，未将边界相接误判为时间重叠。
- 将首个跨午夜冲突放入目标60/1140的课程计划，再加18个合法其他类日期：忽略时间相容性会声称1200，精确目录结果只有1140；加入同日较晚替代项才达到1200。这里各时段“合法”是设计算例的明确假设，没有对应真实已发布课程。

结果只证明目录内可达性与反例；真实时段合法性来源、目录覆盖证据和性能仍NOT EXECUTED。〔本轮Python内存设计核对输出；A-PLAN-01～04；S-R；AT-22。〕


### 2.11 规模、停止搜索与恢复证据

**A-SCALE-01（状态与见证上界）。** A-13的上界是公式范围，不是测得容量。整数剩余目标满足R₀＋R₁≤1200时，最大类别状态乘积为601×601＝361201；K为2/3/4时，单层状态粗上界分别为1083603、1444804、1806005。每类选中记录除最后一条可能仅分配剩余额度外，前面的正贡献至少为H，故见证条数不超过 `ceil(R₀/H)+ceil(R₁/H)`，并同时受日期数及周名额约束。枚举1201种和为1200的整数拆分，H=30/45/60的该上界最大值分别为41/28/21；较少剩余额度不会提高上界。这不表示所有状态都能同时到达，也不表示真实课程有足够日期。〔A-04/07/08/13；S-A/S-R；AT-09～14/22；旧设计§5.7；本轮纯公式枚举。〕

**A-SCALE-02（存储成本边界）。** A-08的成员位序最多占N位（N沿A-13指候选数，仅q>0者分配位序），不能因最终只选至多41条而把每个大整数视为固定大小；见证元组复制也有成本。若后续使用父指针，仍须核算保留的历史节点和回溯链，不能只计算当前层就宣称所有内存为常数。计划目录算法A-PLAN-03另包含最近结束instant；M个选项至多产生M＋1种结束值，因此其状态粗上界还要乘该因子，不能直接套真实候选DP的容量结论。后续需由实现Owner选定资源预算与保留策略并用真实分布验证，本稿不设置未经测量的毫秒、内存或最大记录数承诺。〔工程分析；A-08/13；A-PLAN-03/04；S-A/S-R；旧设计§5.7/8；ISS-016。〕

**A-SCALE-03（搜索未完成）。** 对同一个完整输入快照，已找到的合法组合给出总量下界，1200只是通用上界；中止搜索、超出资源预算或只完成部分日期时，不能把当前最好值当成精确最优值发布，也不能将失败写成0进度。即使已找到1200，总量已最优仍不自动证明原组合优先和开始instant/稳定ID平局已满足；只有对应选择证据也完整，才可进入A-22的受保护发布。尚未完成时保留原检查点及其原来源，显式保持待重算，不能把旧值标为已反映新来源。这里不新建Contract状态。发布计划目录的目标不同：已有合法1200见证即可证明存在可完成计划，不要求挑出真实记录平局最优；找不到见证且搜索未完成仍按A-PLAN-02记证据不足。〔A-06/10/19/22；A-PLAN-02～04；S-A/S-R；AT-10/13/22；旧设计§5.7；ISS-016。〕

**A-SCALE-VERIFY（两类优化反例）。** 本轮只对新增反例调用未改动的A-08，未重跑前轮随机样本：

- 提前停止：认证540/540，K=3，9月7日10:00相关30与12:00其他60，9月8日相关60，9月9日其他60。搜索只到8日时选7日较晚其他60＋8日相关60，总1200；完整输入、没有已发布原组合时，按既有平局规则选7日较早相关30＋8日相关30＋9日其他60，同样1200但优先级更高。搜索前缀不是已发布前驱，不能临时拿它占用“保留原组合”优先权。
- 同日仅留最长：认证570/600，同日10:00相关30、12:00相关60，均合法有效且无原组合。完整选择优先较早30；只保留最长60也得到1200，但选中ID不符。若较晚60本来就是仍最优的原组合，则又应保留较晚60；优化不得删掉原组合候选或把平局改成最长优先。

以上两组输出均保留正确总量，却能暴露选中组合错误，因此后续性能优化须核对完整ID与实际分配，不能只比总量。A-08当前没有采用这些错误优化，本轮不另写一套生产选择器。〔本轮内存计算输出；A-07～10；S-A；AT-10/13；ISS-016。〕

**A-RECOVERY-VERIFY（相同现状、不同历史）。** 认证570/600、同日较早相关30与较晚相关60，最新候选集合完全相同：历史H1先只有较晚60并计入，后加入较早30，仍保留较晚60；历史H2先只有较早30，后加入较晚60，仍保留较早30。两条输入历史按A-08重算均为1200，选中ID却不同。H1恢复时若丢失检查点并把原组合置空，会错选较早30；提供H1真实持久检查点后，重算完整输出与H1一致。该算例验证A-19A的必要性，不证明真实事件日志可重放或备份可恢复。G2只引用这一统计恢复条件，不再定义平局规则。〔本轮内存计算输出；A-10/19/19A；S-A/S-G2；AT-13/25；旧设计§5.7/7.1；ISS-017。〕

## 3. C · 材料版本、一次补证与游泳时限（要点级）

### 3.1 P4Z.C.MaterialVersionFact（唯一材料版本定义）

**C-01。** 正常流程同一 Record 最多两版材料：首次version1与一次补证version2。拟议 `material_version` 记录同一Record/Session/规则版本、version_no、原版引用、服务器材料清单锁定事实、材料就绪事实、受理命令与B退回事实引用；`material_version_asset` 记录版本/资产关联、展示顺序和游泳前后阶段。每版清单锁定后只读，原版及媒体不可覆盖/物理删除；补证以新版本追加，不产生Session、Record或新次数。`UNIQUE(record_id,version_no)`、version_no∈{1,2}、同命令幂等和同记录复合引用是拟议保护，尚无DDL。〔S-C；总览 §11.4/12.3；旧设计 §5.4/6/7.5。〕

**C-02。** 每个当前送审版本独立校验：0～6张JPEG/PNG照片、0～1段MP4视频、至少一项有效媒体；单图≤10MB，视频1～15秒（含端点）、必须含音轨且≤100MB，每版合计≤250MB。容量转bytes沿用旧设计10×1024×1024等既有单位口径。限制计算当前版本引用的全部不同素材，包括原素材引用；物理存储同一素材只保存一次，不跨两版重复计素材，不能误将两版历史相加限制为6图。即使6图＋1视频的各项上限已更严格，仍保留250MB总量校验。〔S-C；AT-21；旧设计 §5.4。〕

**C-03。** 新版合法引用原版asset_id并追加同一次运动的相关材料；版本内同一asset不重复计数，拟议 `UNIQUE(version_id,asset_id)` 与版本内position唯一。资产技术生命周期仍由 `media-evidence` 所有，C只保存业务包关联，不复制资产本体，不用checksum相同自动合并其他业务素材。旧 `media_asset.record_id` 的单业务归属可继续，需补版本关联；旧 `(record_id,position)`、每Record单视频/包计数改按版本校验。系统/AI/Review仅绑定明确版本，由B负责防止旧回调覆盖新版，C不定义AI或审核状态。〔S-C/S-BF；AT-04/07/21；旧设计 §5.4；模块边界 §2.1/4。〕

### 3.2 补证门禁消费（不复制 B/F 计时定义）

**C-04。** 版本2受理必须引用B已确认的一次退回事实与 `P4H.B.SupplementTimerFact` 的当前可受理结果。退回所选24/72是总窗口（默认24），其起算依据是服务器确认退回；C不另存一套固定“退回＋小时数”作为最终截止，不计算维护暂停。学生看到的最终截止/剩余/暂停事实只来自B，并引用 `P4H.F.MaintenancePauseFact`；教师SLA轮次只引用 `P4H.B.TeacherSlaRound`。〔S-C/S-BF；AT-05/08/20；总览 §12.3/12.4/18。上述为依赖要求，不是第二份计时字段或算法定义。〕

**C-05。** C与B消费/发布受理事实时须保证一个逻辑原子结果：B确认仍可受理且版本2合法锁定/受理后，才记录同一次补证成功；重试返回同一结果，不生成version3，不重复消费机会。真实逾期时由B追加系统无效并终结，C据B终结结果关闭后续补证受理/上传入口；补证后被教师判无效同样直接终结，不能再次退回或再交材料。已按时正式受理的补证不因教师晚审而逾期；纠错只追加已有事实判断，不重开材料版本或次数。具体锁和轮次字段留B/后续并发设计，不由C自行判断“尚有多少时间”。〔S-C/S-BF；AT-06/07/20；BD-20260904-01/02；旧设计 §7.1/7.5/7.6。〕

**C-05R（P0引用整改复核完成）。** S-H0的资格矛盾及暂停记录保留在ISS-020。B Owner已在[H §3.1](p4-design-huang-v81.md#p4h-b-supplement-opportunity)、§7.2/9.3/9.4统一一次退回动作与版2受理生命周期，Z按§8.9核对正常一次补证、原命令重放及禁止第二次补证，解除本条引用暂停。C-04/05继续消费B公开资格/终结结果，不存储第二份机会标记、不实现B状态判断，也不复制其计时结构。材料版本仍只在C-01～03定义，受理原子边界仍由C/B协作；实际事务未执行。〔S-H上述位置；S-C/S-BF；AT-05～07/20；任务§4；ISS-020。〕

### 3.3 游泳与首次材料边界

**C-06。** 每版游泳送审包必须有本次运动前、运动后照片各至少一张，合计2～6图并逐项标记阶段；可附最多1视频，格式/大小沿C-02。前后证据关联同一会话，不以事后新拍冒充前照，不要求禁拍区取证、露脸或定位。补证引用原前后证据并添加相关材料，不能用重拍前照补造原事实。〔S-C；AT-19/20；总览 §11.5。〕

**C-07。** 游泳正式服务器结束事实为基准：结束后15分钟内服务器受理并锁定该批材料清单；已锁定同批文件在受理后30分钟内续传，不更换文件、批次或运动日期。清单锁定/续传与完整媒体就绪不同：未传完或未核验就绪不得作为可审核的有效材料交B。完全离线可在结束后24小时内带已有会话/材料与延迟说明进入异常队列；不能凭客户端时间/截图自动通过、倒改结束或增加时长。补证不再套结束后15分钟，而消费C-04的B计时结果；普通运动不套15分钟。〔S-C；AT-19/20；旧设计 §5.4/7.5。〕

**C-08。** 旧设计“先全部VERIFIED才提交Record并立即VALID”不能直接承载锁定后续传。C提出清单锁定事实与材料就绪事实分离；Record行创建时机、普通首次材料明确最晚时点、跨收尾/成员移出的首次受理与传输终结属于 `P-20260904-02`，本稿不冻结这些分支，不假定首次受理永远等于传输完成。课程关闭本身不截短关闭前合法会话已适用的首次/续传窗口；成员移出、跨收尾/归档不能从关闭规则推导全部放行。PENDING不阻塞本稿记录已确认的15/30/24规则与材料版本结构，但阻塞相应实现。〔S-C/S-E/S-P；AT-19/24；总览 §8/11.5；学生 §6.3；旧设计 §7.5。〕

### 3.4 C 的 AT 映射与反例

**C-AT19 / AT-19。** 10:00服务器结束，10:14:59受理锁定，有合法前后照；同批在10:44:58传输完成并核验可继续入审核，未完成前不贡献进度。10:15:01才尝试普通首次受理不能伪称15分钟内成功；若完全离线且满足24小时内说明规则则交异常队列，不自动有效。缺前照不以新拍照片补造；受理后换文件不能冒充同批续传。恰好边界的服务器比较精度与传输终结协议留对应受理设计，不用客户端时间制造证据。〔S-C；C-06～08。文字推演，真实上传NOT EXECUTED。〕

**C-AT20 / AT-20。** 游泳运动结束数小时后教师已合法退回一次补证，只要B提供当前可受理事实且本版仍含原前后证据，就按24/72总窗口处理，不再用“结束已超过15分钟”拒绝。若B确认已按时正式受理，则等待教师新轮次；若B确认逾期终结或补证后判无效，拒绝继续受理该原记录材料。C不复算教师迟审或维护时间。〔S-C/S-BF；C-04～07。〕

**C-AT21 / AT-21。** 当前版第7图、第二视频、空包、单图超10MB、视频0.9秒/15.1秒/无音轨/超100MB均不能形成合格包；1秒或15秒含音轨且满足其余约束可接受。首版6图，新版引用其中2图＋新增4图，当前版6图、历史不同素材10个，可按版本校验；不得按两版共10图拒绝，也不得复制引用素材存储。申请第4图仍按同一申请累计≤3拒绝，不能因为运动包≤6而放宽；免测/认证业务不由C重新定义。〔S-C；总览 §11.4/14；教师 §10.3；旧设计 §5.4/5.6/7.8。〕

## 4. E · 邀请宽限、课程关闭与结算（要点级）

### 4.1 邀请注册流程登记

**E-01。** 邀请默认30分钟，允许5～120分钟，二维码和手动码同规则。新增的逻辑事实是 `invitation_registration_flow`：由 `course-enrollment` 保存邀请/课程/学期、不可伪造的同一次注册流程关联、服务器登记时间、该邀请自然到期快照、完成/终止事实与幂等命令引用。它不等于Enrollment或注册成功，不因预览、扫码、本地草稿或页面打开自动生成宽限资格，也不额外收集未要求的学生身份信息。旧邀请码digest不存原文原则继续。〔S-E；AT-16；总览 §9.4；旧设计 §5.3/7.4。工程事实名，非新API定义。〕

**E-02。** 只有自然到期前已由服务器登记的同一次注册流程可在自然到期后再完成10分钟；宽限固定于原expires_at，不从刷新、重试或重新打开页面重新起算。登记晚于/等于自然到期的流程没有宽限，不能在到期后新建登记补票。最终成功最多一次，重试返回原结果而非再次入班；撤销、入班关闭、课程关闭、学期失效立即终止未完成宽限。完成前仍由原身份及课程Owner复核合法身份与同学期唯一有效班级；宽限不豁免这些校验。〔S-E；AT-16；旧设计 §5.3/6/7.4。〕

**E-02A（本会话已确认的截止等号）。** 周润基已明确选择“采用严格早于，截止时刻拒绝”。对符合E-02的原注册流程，`grace_end=原expires_at+10分钟`，首次成功完成所依据的服务器受理instant必须满足 `accepted_at < grace_end`；等于或晚于grace_end拒绝。自然到期前登记资格仍为 `registered_at < expires_at`，不因本次明确宽限终点而放宽。比较使用同一权威服务器时间口径及E-03的当前受保护事实，不以客户端发起时间、HTTP返回时间或显示取整替代。原已在期限内成功的命令，期限后幂等返回原成功结果不构成再次入班；不存在原成功事实时不能用重试补造期限内受理。本决定只在E定义，H/G3的GAP-H19待Owner同步；未修改业务正文或Contract、不新建BD号。〔本会话对截止时刻的明确答复；S-E；总览§9.4；AT-16；E-02/03；S-H §12.3 finding 6、GAP-H19；旧设计§7.1/7.4；ISS-019。〕

**E-03。** 并发设计要求登记/完成与撤销/关闭共享一致版本保护，最终写Enrollment时复核当前邀请、Course、Semester及流程状态，不能只信10分钟前校验结果。绑定流程的command唯一、同一流程至多一个成功Enrollment关联、原到期时间不可被重复请求刷新；课程关闭使未完成流程立即不再可用，但不撤销已有合法成员。具体登记操作是否新增及其协议由Contract Owner评审，旧预览GET标为READ_ONLY，不可暗中借其写登记。〔S-E；AT-16；旧设计 §7.1/7.4；operation-catalog相关邀请行。〕

**E-AT16 / AT-16。** 12:00发默认邀请→自然到期12:30；流程A在12:29:59被服务器登记，可以在12:39:59完成一次，恰好12:40:00及12:40:01均拒绝。流程B在12:30或之后才登记，或仅有本地草稿，没有宽限资格；刷新A不顺延。若12:32撤销或关闭入班/课程，A此后立即不能完成，即使未到12:40；原已加入成员不受二维码自然到期影响。参数5和120在范围内，4/121拒绝。E-02A决定后的8项内存时间谓词核对均得到预期结果：截止前1微秒允许、等于/晚于截止拒绝、登记等于/晚于自然到期无资格、合格旧流程在自然到期时仍可完成、宽限内终止即拒绝、12:39:59允许。微秒仅用于区分邻接instant，不规定数据库时间精度；身份校验、幂等、事务和网络仍只是文字约束，真实并发NOT EXECUTED。〔S-E；E-01～03；本会话端点确认与本轮8项时间谓词输出；ISS-019。〕

### 4.2 课程关闭和七天收尾

**E-04。** 保留Course OPEN/CLOSED与学期生命周期，关闭事实独立保存服务器确认时间/版本。关闭只禁止新成员、新会话、新免测/认证申请、重新开放邀请或补练。关闭前合法对象的继续处理必须关联该对象及其服务器确认事实：已有会话暂停/恢复/结束、该次首次材料、已锁定同批续传、异常审核/一次补证、已受理名单/OCR/申请、收尾结算均按原规则继续。不把本地未确认草稿升级为合法起点。〔S-E；AT-24；总览 §8；旧设计 §5.3/7.4。〕

**E-05。** 关闭≠常规截止≠结算≠归档；关闭不清空待办、不自动无效、不缩短原有效期限、不增加补证机会。旧设计 §5.3 的关闭后拒绝全部教学mutation被上述边界替代。操作入口应基于“新起点/合法旧链”及各事实Owner原资格校验，不可只写 `Course CLOSED→拒绝全部写`。C的首次/续传事实、B/F的审核/计时/维护只按OWN-02引用；P-20260904-02的首次受理特殊边界仍未冻结。〔S-E/S-C/S-BF/S-P；AT-24；学生 §6.3。〕

**E-06。** 发布时固定常规截止、其后七天收尾期和结算安排。截止前14天提醒；发布距截止不足14天立即提醒，不伪造过去已发送通知。收尾期仅教师明确指定学生和窗口的补练可创建会话，仍要求原学期当前、课程开放、成员有效，使用原真实日期/周和A日/周上限。不自动对全员开放、不倒填旧周、不因推迟结算自动延长补练。补练对象/窗口与确认时间留在课程Owner事实中，不以日/周进度未满自行授予资格。〔S-E；AT-23/24；总览 §21；教师 §5.2/12.2；A-21。〕

### 4.3 P4Z.E.SettlementVersion（唯一结算版本定义）

**E-07。** 结算由课程Owner编排；名单/OCR、申请、审核、媒体、Session、统计各Owner提供公开阻塞/事实投影，禁止直接跨模块查私有表。先逐项检查未结束会话、材料传输、技术任务/故障、待审、有效补证窗口、名单/OCR、免测/认证及其他教学待办；存在未完成或尚有效期限时展示具体对象与原因并延后结算，不能批量无效清空。已按时受理且已审核完成的补证无需空等旧截止；B提供是否仍阻塞的事实，E不计算学生倒计时或教师SLA。〔S-E/S-BF；AT-23；总览 §21；教师 §12.2；学生 §8.3；模块边界 §4。〕

**E-08。** 拟议 `settlement_version` 归 `course-enrollment`：课程/学期、版本号、前版引用、正常结算或受控更正的来源关联、教师确认/原因/服务器时间、综合名单/成员范围快照引用、A的统计投影版本、各待办Owner来源版本、报告版本与当前指针。一个版本对应一次完整确认，版本/命令唯一、前版必须属于本课程、旧版本不可覆盖。最终冻结前复核阻塞和来源版本仍相同，变化则重新检查，不能在“无待办检查”后悄悄接受新事实而仍发布旧报告。此为课程收尾报告，不转移 `grading` 的最终成绩发布权。〔S-E；AT-23/25；旧设计 §5.3/5.7/7.1/7.2；模块边界 §2.1/4/5。工程落点。〕

**E-09。** 报告列引用 `P4Z.A.StatisticsProjection`（A-14～19）：实际运动时长、有效计入（认证与运动来源分列）、实际无效分钟、有效未计入、公式未纳入量解释、两类剩余目标和达标状态。不得在E另写统计公式。报告另外引用未完成原因/待处理事项和报告版本；零待办不等于所有学生达标，未达标可真实结算，不能补造分钟。上限截断不是违规失效，无效记录的可计量也不是总进度减少量。客户端可見范围引用既有角色规则，学生仍不见最终分数、换算分、等级、排名；最终成绩无备注缺口只转交P4-H，不在报告中新建成绩备注替身。〔S-A/S-E；BD-20260904-02；AT-12/14/23/25；总览 §15/21；教师 §12。〕

**E-10。** 旧学期课程完成收尾结算后，`academic-term` 的切换事务才能归档旧学期并建立唯一当前学期；存在具体阻塞时拒绝切换，不通过批量判无效绕过。已结算或归档后，只对已有事实追加有原因、有操作者、有原对象引用的受控更正，重新取A投影形成新版报告并链接原版；旧事实/旧报告不覆盖，不恢复普通运动、补练、正常补证或改课程规则。学期配置仍单向只读，不恢复/编辑/删除归档学期。本版不建有限接管、学期中删除责任教师或责任转移模型。〔S-E；BD-20260904-02；AT-25；总览 §7.1/21；旧设计 §5.3/7.2。〕

### 4.4 E 的 AT 映射与旧设计差异

**E-AT23 / AT-23。** 结算计划到达但仍有未结束会话、未传完材料、B确认仍有效的补证窗口、待审或技术故障→逐项阻塞并延后结算，不批量判无效、不延长补练；若补证已正式受理且完成审核、其余阻塞均清除→可确认结算，无需等最初24/72窗口走完。14天提醒：9月30日常规截止→9月16日提醒；9月20日才发布→立即提醒，不回填9月16日发送记录。〔S-E/S-BF；E-06～09。〕

**E-AT24 / AT-24。** 10:00合法会话，10:10课程关闭→该会话后续结束、已有规则范围内首次材料/续传、之后一次补证与审核继续；10:11新会话、新邀请入班或新申请拒绝。移出成员后既有审核待办不消失，但不得据此放行新的运动；移出后的尚未受理首次材料边界保持PENDING，不从课程关闭类推。关闭时有待办不等于已结算，归档门禁保持。〔S-E/S-P；E-04/05/10；C-08。〕

**E-AT25 / AT-25。** 归档后有权限教师依据已有事实纠错→保留原判断与结算v1，追加判断并引用A重算结果生成更正版v2，v2关联v1及原因/操作者。反例：覆写v1、直接改原时长/规则、重开原记录补证、恢复学期或授权其他教师接管，均不得发生。〔S-E/S-BF；E-08～10；AT-25。〕

**E-11。** 差异清单：旧设计 §5.3/7.4 邀请只有简单未过期条件→增加E-01～03的登记与一次宽限；§5.3关闭后所有教学mutation拒绝→E-04/05的新起点与旧链；§7.2仅切CURRENT/ARCHIVED→增加课程结算阻塞检查；§5.7仅成绩发布/即时统计→新增E-08的独立结算/报告版本。旧名单、OCR、申请、成绩模型未在E重构，只保留公开来源引用。〔S-E；AT-16/23～25。〕


### 4.5 结算快照与A投影的一致性补强

**E-08A（冻结清单）。** E-08的报告准备清单必须引用课程/学期、正式成员范围版本及完整Enrollment集合、各Owner阻塞/事实来源令牌、每名成员对应的A投影ID与输入令牌、A策略版本和报告前驱。不能仅保存一个“当前课程总量”，也不能将多个时点读到的各学生最新值拼成同一版。正式成员集合由课程Owner提供；报告准备清单与统计行都被截断时，二者彼此相等仍不表示完整，必须与Owner给出的完整集合再核对。名单/OCR的正式事实继续只引用其Owner，不在E重新定义匹配口径。〔S-E/S-A；AT-14/23/25；总览§11.3/21；教师§12.2；A-18/19/22A；旧设计§5.3/5.7；ISS-015。〕

**E-08B（冻结前复核）。** 已通过原权限/资格校验后，先取得完整Owner结果；任一读取失败、未知或只返回部分结果都不是“无待办”。在A-22A/22B规定的来源保护原则下，于同一受保护提交点检查成员范围和来源令牌未变、前驱报告未变、无合法待办阻塞、统计行与正式成员集合一一对应且不重复、每名学生输入令牌完整并匹配、课程归属及A策略版本正确，才原子追加结算/报告版本并更新当前指针。投影尚未反映某名学生最新来源时等待重算，不以旧值或0填洞。B侧审核来源保护及完整审核阻塞已对应S-H §6.2/6.3；其他Owner范围仍须分别提供，不能要求B拼装全部结算来源。真实提交未执行，补证受理条件矛盾另受C-05R/ISS-020限制。〔S-E/S-A/S-BF/S-H；AT-23；E-07/08；A-19/22A～22C；旧设计§7.1；模块边界§4；ISS-015。〕

**E-08C（检查与变更的顺序）。** 准备阶段“无待办”之后发生新候选受理、补证就绪、认证变化、名单/成员变化或更正，使任一相关来源令牌变化，旧准备结果不得冻结为当前报告；须取新来源重做对应投影/检查，不能仅改报告生成时间。若另一任务先发布了下一版，则前驱CAS拒绝覆盖；重试已完成命令可返回原版本及其来源，但不能把旧报告切回当前。这些规则复用A的原子来源保护，不在E定义另一套B/F计时或审核状态。〔S-E/S-A/S-BF；AT-23/25；A-22A～22C；E-08；旧设计§7.1；ISS-015。〕

**E-08D（快照核对算例）。** 本轮用内存快照模型逐项核对15种情形，均得到设计要求的结果：两名学生进度1199和600、其余来源完整且无待办时，快照可通过核对，未达标本身不能被当作未处理待办；14项负例分别为缺统计行、重复统计行、学生旧投影、成员范围版本变化、新来源进入、Owner给出待办、Owner读取不完整、报告前驱已变、其他课程行混入、A策略版本不符、准备清单重复、准备清单与统计行一起漏人、缺学生来源令牌、Owner成员清单异常，均停止此次冻结并给出对应缺项/变化原因。模型只核对公开结果间的完整性和版本关系，不实现鉴权、B判定、计时、数据库CAS或真实并发；不得将“快照核对通过”写成已完成结算。〔本轮15个内存场景输出；S-E/S-A；AT-14/23/25；E-08A～08C；ISS-015。〕

**E-10A（归档与更正版引用）。** 学期切换仍由 `academic-term` 消费课程Owner的完整收尾/结算阻塞结果；不能只查到某门课程“曾有一个报告”就归档整个旧学期，也不能把集合读取失败当作空课程列表。归档后纠错应引用纠错对象对应的历史范围及其Owner提供的新事实版本，生成链接前版的更正版；不得简单用当前有效成员列表替代历史报告范围，导致已移出学生在更正时丢失。原报告保留其原输入与数值，A缓存刷新不能覆盖它；更正原因属于受控纠错记录，不是新增最终成绩备注。〔S-E/S-A；AT-14/25；总览§7.1/21；教师§12.3；A-18/19A；旧设计§5.3/7.2；E-08/10。〕


## 5. G2 · 迁移与恢复边界

**G2-REF。** G2唯一详述在 [p4-migration-note.md](p4-migration-note.md)，覆盖空库首个migration直接新结构、目标环境只读盘点、发现既有数据触发 `DATA_MIGRATION_DECISION_REQUIRED`、禁止名称/旧subtype/默认值猜测回填、备份与恢复点、旧模型不可逆回退、技术恢复不得改判和所有动作NOT EXECUTED。A/C/E只提供经接受设计后的结构需求，不在本稿另造迁移步骤或数据映射。〔S-G2；任务G2。〕

## 6. Contract 与模块边界只读登记

**CR-REF。** 本轮只把 [operation-catalog](../../../contracts/operation-catalog.md) 的相关操作作为影响定位，未验证schema字段足够，也未新增API：

- Courses：`createCourse`、`updateCourse`、`previewCourseChangeImpact`、`closeCourse`。规则发布/锁定、关闭后旧链和结算事实须另行评估承载能力；不能由操作名推定已有发布/结算协议。
- Exercise：`startExerciseSession`、`pauseExerciseSession`、`resumeExerciseSession`、`completeExerciseSession`、`submitExerciseRecord`、记录读取与 `appendExerciseRecordReview`。日/周计入不可充当开始门禁；材料版本/中间状态/受理锁定不能假定旧提交操作已表达。
- Statistics：`getOwnCourseProgress`、`getCourseMemberProgress`、`listCourseProgress`。待表达A三分列、选择版本、原因和正确成员范围；不回用旧直接求和结果。
- Invitation：创建/撤销/预览、`joinCourseByInvitation`、`registerStudentAndJoinCourse`。预览为READ_ONLY，不是注册流程登记；新事实怎样承载由Contract Owner决定。
- Semester：`getCurrentSemester`、`listSemesters`、`createSemester`、`updateUpcomingSemester`、`switchCurrentSemester`。切换须消费结算阻塞，不能以旧签名存在推断门禁实现。

版本仍 `1.2.0-contract / RC`；`CR-20260901-005` 与最终成绩 `remark maxLength:50` 已知缺口保持，仅引用任务输入/STATUS，不重读相关schema、不在此创建替代缺口清单。P4-H负责正式Contract差异归集。旧API/Mock现状只引用STATUS，未开展全仓审计。〔S-A/S-C/S-E；Z-CONTRACT；operation-catalog相关行；模块边界 §2.1/4/6。〕

**CR-H-REF（只引用正式缺口位置）。** ISS-006已对应[S-H §17 G3](p4-design-huang-v81.md#p4h-g3-contract-gaps)：A引用GAP-H01/H06，C引用H04，E引用H07/H11及历史精度项H19，B/F时间依赖引用H05，原remark缺口引用H13，CR-005独立性引用H20。这里只登记索引，不复制缺口定义或宣称schema已由Z核验。H19已按E-02A关闭设计精度问题，剩余Contract表达归入H07；此单项变化不解除本批次Phase5总门禁。其他缺口处置、新RC与下游验证仍未执行。〔S-H §12.3/17/18.4；S-A/S-C/S-E/S-BF；CR-REF；Z-CONTRACT；ISS-006/019。〕

## 7. PENDING、跨 Owner 确认与后续工作

**PENDING-01 / P-20260904-01。** 六类均不适用时的终局标准尚未确认；不把疑虑当已证实冒用，不默认通过。由B及业务Owner解决，A只消费明确有效候选，不阻塞当前选择公式设计。〔S-P/S-BF。〕

**PENDING-02 / P-20260904-02。** 普通首次材料最晚时点、跨收尾/成员移出的首次受理与受理后传输终结尚未确认。C-08/E-05已圈定未冻结分支，不扩大游泳15分钟或课程关闭旧链规则，不提供该分支DDL或执行方案。〔S-P/S-C/S-E。〕

**PENDING-03 / P-20260904-03。** 学校统一工作日日历来源与日内边界尚未确认；不等同48小时或普通周一至周五。E只消费B待办/阻塞结果，本稿不写教师SLA计算器。〔S-P/S-BF。〕

**PENDING-04 / P-20260904-04。** 非维护故障在错误逾期写入后才确认的补救尚未确认；不据此重开真正逾期记录、不重置补证机会。维护已确认规则引用P4-H F/B，C/G2不补造事后恢复语义。〔S-P/S-BF。〕

**CONFIRMED-Z。** 三项会话决定已由周润基明确答复：A-07认证优先、再按开始instant/稳定ID分配；A-17有效未计入仅q-a，m-q不混入；E-02A邀请宽限采用严格早于、截止时刻拒绝。本轮草稿接受记录见§10.9，保留原问答及ISS-009/010/019证据。四项既有PENDING仍开放；无新增BD，正文与Contract未改，正式REQ/Phase2对应仍需补齐。〔本会话三次明确答复及接受指令；S-A/S-E；Z-AUTH；ISS-009/010/019。〕

**OPEN-OWNER（当前剩余范围）。** S-H修订后八项设计引用对应；ISS-019/020/021已在设计范围闭合，周润基已接受Z v9与G2，当前进入联合收尾。四项业务PENDING、各Owner真实来源保护/恢复能力及完整DoD仍开放；B/F恢复清单只引用[H §12.4](p4-design-huang-v81.md#p4h-z-g2-review)。H正式文件落点、Handoff/STATUS和对联合汇总的回复由H侧完成，状态见[联合增量汇总](p4-design-deltas-v81.md)。〔任务§4；OWN-01～03；S-H §12/18；本会话接受指令；§10.9。〕

**FOLLOW-UP（继续留在Phase 4）。** 周润基已接受约定深度交付并启动联合收尾；本次v10仅登记接受、当前协作状态与证据，不改A/C/E设计。黄友晟已Review的Z v8/G2及Z v9接受版本身份见§10.9；后续实质设计改动仍按变化范围Review。真实计划目录、统计并发/性能/重放、C/E关键事务与未决受理分支、环境盘点与迁移决定、备份恢复、Contract及Phase3 Handoff/REQ/Phase2正式对应仍留后续。联合汇总由Z唯一编辑，H侧正式文件与正式交接按原Owner处理；本批次始终PARTIAL。〔任务深度/路径/门禁；S-R/S-C/S-E/S-G2/S-H；Z-AUTH；§10.9。〕

## 8. 设计检查与 Review 记录

### 8.1 已执行的算术核对

**CHECK-P0-A：PASS（设计计算）。** 在内存运行A-08参考算法，核对AT-09～14及反例；没有导入、调用或修改产品代码、数据库或Contract。实际输出要点：AT-09为0/30/45/60；同日30+60选60；默认周4条30/45/60/60选165；跨午夜总240、跨周补证总240；达标后记录实际计入0、总1200；旧30补证不替换60；认证570/500时总1100、撤销相关认证后总680；跨类别每日贪心反例精确结果1199。还核对30/45/60门槛、原组合优先、开始时间优先、稳定ID平局、35分钟非阶梯值与认证不占名额。〔A-08与§2.7；本轮Python内存算术输出。〕

**CHECK-P0-ORACLE：PASS（120组小规模设计样本）。** 使用Python固定随机种子40914，生成120组0～8条候选、两个业务周、H∈{30,45,60}、K∈{2,3,4}，目标含0/30/60/90/600/1110/1200边界，ACTIVE整分钟样本29/30/35/45/59/60/90。独立oracle穷举全部子集，仅按不同业务日期/每周名额过滤，再直接代入A-06分类封顶式取最大值，与DP全部一致；非空样本同时核对新增末条候选后的总进度不下降、逐类认证贡献＋记录实际计入等于分类完成量。样本用于抽查实现参考与公式一致，不代替A-09～12的证明，更不是产品负载/状态/并发测试。〔本轮内存算术执行；A-06～12。〕

**CHECK-P0-OWNER：PASS（本批次设计引用范围）。** 首轮单侧核对、v8发现及暂停保留在§8.8；本轮依据S-H修订、8项逐项回复与G2精确Review，并完成§8.9的定向流程推演，八项引用均对应。A不定义有效候选，C/E不复制B/F计时，G2不另设恢复语义。此PASS不覆盖真实来源保护、H整包或尚未确认的业务分支。〔OWN-01～03；OPEN-OWNER；S-H §12/18；C-05R；§10.6。〕

### 8.2 成稿后的定向 Review

首轮成稿的AI定向Review已完成；以下数量为首轮快照，续作记录见§8.4/8.5与§10.5，不代替周润基接受或黄友晟Reviewer确认。

- **P1：PASS（本轮范围）。** 两份文件共92个编号结论块（本稿74、G2稿18）均有来源标记；逐节对照§1来源索引、已读正文和旧设计定位，保留四项PENDING与两项会话确认。15个只读输入/既有文件的SHA-256与写入前一致，包含根AGENTS、STATUS、指定业务/旧设计/交接、Contract metadata/catalog及三项DS文件。Git核对仅新增两份允许文件，原三项DS保留，未有其他已跟踪变更，暂存区空，HEAD/分支不变。没有把未扫描的全仓文件声称为逐个哈希核验。
- **P2：PASS（文字映射）。** C的AT-19/20/21、E的AT-16/23/24/25均有正反例或边界推演；A-24/25、C-03/08、E-11逐项标注旧设计冲突位置。既有四项未决分支未用默认值补齐；AT-26/27仅作已作废引用，不进入验收。未执行C/E真实流程。
- **P3：PASS（仅新文件机械检查）。** 20处本地链接/所带锚点可解析；两份文件无行尾空白、代码围栏闭合、UTF-8可读，A-08提取后语法编译成功，且与已执行算术核对的代码逐字一致。算法文本SHA-256为 `3dea65c0ad1fc5425e7127684ed3b7b780855748cd2739648e88c53f695257d8`。`git diff --check`返回0，但新增文件尚未跟踪，因此对两份新文件另做逐行空白检查，未将空diff当作新文件检查证据。

Review证据边界修正：§2.7跨午夜/成员移出/达标继续记录的准入与历史保留是文字推演；内存算法只消费给定的B候选和原业务日期，未模拟服务器计时、审核、成员操作或真实时区持久化。P0算术通过不表示这些业务能力已运行。其余算法正文未改，不无故重跑已通过的120组样本。〔本轮文档、源码比对、哈希和Git工具输出；§2.7/8.1。〕

工具记录：一次草稿写入调用在JavaScript解析阶段因Markdown反引号转义失败，未执行文件写入；更换字符串封装后写入成功。一次只读文档检查因生成的Python正则字符串含意外换行而语法失败；仅改为代码围栏分段提取后检查成功。两次故障之间有成功调用，未发生同一种方式连续两次失败；均为检查/写入包装问题，不是产品、环境或业务算例失败。〔本轮工具输出。〕

### 8.3 未执行的检查

产品构建、单测、E2E、真实上传、AI、计时、审核、并发锁、数据库连接、DDL/migration、数据回填、备份/恢复/回退、部署、性能与真实课程可完成性均 `NOT EXECUTED`，因为本阶段是纯设计且未获实现/环境执行授权。Contract verify和历史Android/Web测试未重跑，相关输入未变；只引用STATUS的上游现状，不把历史数字列为本轮成绩。未做全仓旧API/Mock/TODO/空接口扫描；H↔Z本轮八项设计引用已完成核对，但未扩展为H整包或正式交付接受。〔任务 §2/5；Z-CONTRACT；OPEN-OWNER；§8.9。〕

### 8.4 续作记录（2026-09-06）

本次依据周润基“建立并持续更新问题记录、继续P4-Z”推进；仅在原设计稿中追加问题记录并定点补齐已登记工程缺项，没有新增范围外文件。

- P0新增设计核对：A-VERIFY-02的72组完整组合独立枚举、216次重排、72次同快照重算、54次移除已选记录对照均通过；A-08算法未改，未重跑首轮120组。
- P0工程细化：A-22A～22C补齐来源集合标识、防止校验/发布之间的竞态、前驱CAS与重复提交边界；A-19A补齐原组合的恢复条件。并发只作顺序推演，实际锁与事务NOT EXECUTED，跨Owner能力仍待ISS-001确认。
- P1/P2新增证据：A-PUBLISH-VERIFY验证AT-22中20日期最大1190与21日期达到1200的条件样例；A-REPORT-VERIFY四组分列算术通过。真实课程可完成性求解器、C/E待决分支和目标环境仍未执行/未冻结。
- P1/P3文件级复核：通过。现有两份文档99个编号结论块均有来源标记，14项问题记录均有状态与来源，21处本地链接/锚点可解析；空白与围栏检查通过。15个只读输入/既有文件、A-08算法SHA与迁移稿SHA均未变；Git仅原两份P4-Z未跟踪草稿和三项DS，分支/HEAD不变、暂存区空。上述是续作文件快照，不替代运行验收。

### 8.5 再续作记录（2026-09-06）

本次依据周润基“收到。继续推进P4-Z”处理ISS-013的时段相容性和新登记ISS-015的结算一致性；只修改相应段落、问题记录及检查报告。

- **P0设计核对：通过。** A-PLAN-VERIFY新增48组有限目录独立穷举对照及跨午夜、PAUSED跨度、相接端点、替代时段、1200误算为可完成的定向反例；E-08D最终15种快照情形均得到预期结果。结算模型定向Review发现“准备清单与统计行一起漏人”和“缺学生来源令牌”仍需防护，补齐核对后执行最终15项；并非真实数据库结算或跨Owner联调。
- **P0所有权：仍为单侧。** A计划目录不是B有效候选，E复用A来源保护并消费Owner待办/成员结果；未定义B/F计时、D名单匹配。正式跨Owner位置与原子保护能力仍待ISS-001，未关闭双向Review。
- **P1/P2范围与证据：** 补A-PLAN-01～04/VERIFY、E-08A～08D/E-10A并附来源。没有找到计划见证时区分“已证明不可完成”和“证据不足”；未达标本身不等于结算待办。真实目录合法性/覆盖性、C的既有未决分支和运行能力保持开放，无新增业务PENDING。
- **P1/P3文件级复核：通过。** 两份文档109个编号结论块（本稿91、G2稿18）均有来源标记，15项问题连续编号、21处本地链接/锚点可解析；无行尾空白，围栏闭合。两段Python参考代码语法编译通过，A-08的SHA保持原值，新计划目录代码与本轮已执行版本一致。15项只读输入/既有文件以及迁移稿，共16项哈希均与续作前相同。Git仅两份允许草稿和原三项DS未跟踪，已跟踪diff与暂存区均空，分支/HEAD不变。首轮120组和续作72组记录选择样本未重跑；这些文件检查不代表产品验收。

### 8.6 规模与恢复边界续作（2026-09-06）

- **P0新增核对：通过。** 使用未改的A-08执行A-SCALE-VERIFY两组优化反例及A-RECOVERY-VERIFY的两条历史/缺失检查点/带检查点重算，实际选中ID与分配符合正文中的推演。未把总量相等误当完整组合相同；没有实现有问题的优化，也没有改动B有效性定义。
- **P1工程证据：完成本轮限定核对。** 对H=30/45/60分别枚举1201种整数目标拆分，正贡献见证条数上界最大为41/28/21；类别状态乘积最大361201。该公式计算不是性能基准。G2-15A～15D补清恢复清单、原报告/检查点保留和停止边界，八项评审情形已逐项对照来源；只有其中统计历史差异有内存计算，其余为文字推演。
- **P0/P2边界：** 所有权仍只完成P4-Z单侧核对，既有四项PENDING及Owner能力确认保持开放；C/E要点范围未扩展成另一套状态机。两个已确认统计口径未改变，未新增业务PENDING；本轮不修改Contract、不执行数据库恢复或性能测试。
- **P1/P3文件级检查：通过。** 本轮仅两份允许文档变化，15项只读输入/既有文件哈希保持不变；118个编号结论块（本稿96、迁移稿22）均有来源标记，17项问题连续编号、21处本地链接/锚点可解析。空白/围栏检查和两段参考代码语法编译通过；A-08及计划目录代码均未变，未重跑已通过的随机样本。Git仍为main及指定HEAD，仅两份草稿和原三项DS未跟踪，已跟踪diff与暂存区均空。§8.4/8.5“迁移稿未变”只描述对应历史快照，不代表本次没有修改迁移稿。

### 8.7 未计入解释与联合引用续作（2026-09-06）

- **P0定向核对：通过。** A-15A～15D补齐公式未纳入与有效未计入的解释范围、部分计入不得归因于自己占用的日/周名额，以及两种明显不一致判据。8项正常投影解释及2项注入不一致均得到预期结果，未实现B判定或产品接口。
- **P0所有权：单侧完成，双向仍待答复。** §10.6整理8项可转交的引用确认清单；只定位正式定义与消费约束，没有读取/修改P4-H稿件，没有发送外部消息或替黄友晟确认。两项已确认统计口径不再次征询。
- **P1/P2范围：** 新增ISS-018，登记为解释工程细化而非新业务PENDING。C/E继续引用A的唯一统计定义；四项既有PENDING、正式Contract差异及Phase3追溯缺项保持原状态。60分钟检查点已在本会话报告完成内容、阻塞、失败归属、剩余范围、下一步与拆分判断。
- **P1/P3文件级复核：通过。** 本轮仅主稿变化；15项只读输入/既有文件及迁移稿共16项哈希未变。两份文档122个编号结论块（本稿100、迁移稿22）均有来源标记，18项问题连续编号、21处本地链接/锚点可解析；空白、围栏与两段参考代码语法检查通过，参考代码内容不变。Git分支main及指定HEAD保持，状态仅两份草稿与原三项DS未跟踪，已跟踪diff及暂存区为空；未重跑旧随机样本。

### 8.8 接收H初轮附件后的定向交叉核对（2026-09-06，v8历史快照）

- **输入版本：已核实。** 本轮修改前Z主稿的SHA为 `71b813f6757b1e19134408305349634a7769a7602ae045a7a697a948103aec29`、735行，与S-H0 §12.1评审对象完全一致；H附件的949行/80152字节和SHA见S-H0。截图记5确认/3待补，附件§12.2记8确认，两份证据分别保留，未凭数量替代逐项核对。
- **P0所有权：未全通过。** 四个正式别名和8项落点均已找到；其中1/3/4/6/7/8在所读范围内与Z消费边界一致，2/5因H §7.2与§9.3/9.4的补证资格相反条件暂停关闭。ISS-020给出位置、影响和Owner回复要求；没有修改H、替B定规则或继续该分支的实现。
- **P1邀请 finding：Z侧已补。** 周润基明确选择严格早于，E-02A/E-AT16已补等号与相邻instant。8项新增时间谓词核对通过，仅为设计计算；H的GAP-H19待其Owner同步，不宣称Contract已解决。
- **G2评审范围：仍缺。** H明确未取得独立迁移稿；Z现有文件SHA为 `55b04e2c198ebe7b824686b9a561c2879aade4bd6510115c71c7bf1add00e6d4`，本轮不改，待按ISS-021转交并做定向评审。H已定义维护来源不等于已评审G2文件。
- **P1/P3文件级复核：通过。** 仅Z主稿变化；仓库内17项其他输入（含迁移稿、业务、Contract及四项DS）和H附件共18项哈希未变。125个编号结论块（Z主稿103、G2稿22）有来源标记，21项问题连续编号，29处本地链接/锚点可解析，含H附件的正式HTML锚点；空白与围栏检查通过。两段参考算法SHA未变，沿用前轮语法和算法证据。Git仍为指定main/HEAD，已跟踪diff与暂存区为空；未重跑A随机样本、H内部检查、Contract verify或任何产品/DB检查。
- **工具记录：** 一次调用在JavaScript阶段因前轮暂存值不存在触发 `Object.keys(undefined)`，未执行读写命令；仅改为本轮显式路径后成功。与此前两次包装语法故障分开记录，没有同方式连续两次失败；不是业务算例或环境失败。

### 8.9 H修订回复的定向闭合核对（2026-09-06，v9）

- **版本证据：匹配。** S-H为982行/85532字节，SHA与23:25截图一致；其所列Z v8与G2完整SHA、行数和字节数均与本轮写入前文件匹配。H结论覆盖该精确v8与G2；本次v9仅吸收回复及更新证据，不把新SHA冒称已由H另行评审。
- **P0核对范围：通过。** 对S-H0→S-H做定点差异核对；有效候选与来源保护§6、SLA/F引用§7.3～7.4、维护§15.5、客户端统计§16.5文本未变，复用原引用核对。变化的§3.1/7.2/9.3/9.4与C-01～05按下面8种最小流程逐步核对。以下是文字状态推演，没有编写、运行B状态机或重复H全量验证。

1. **首次合法退回后提交版2：通过。** 先按H §9.3产生唯一退回事实及Timer，再在截止前提交同记录合法版2；H §7.2与§9.4的新受理前提一致，C锁定后与B原子绑定版2/round2，原相反条件不再阻止正常路径。
2. **同一退回命令重试：通过。** 依H §9.3返回原Timer，不再次退回、不增加Timer或材料机会。
3. **已受理命令晚到重试：通过。** 首次版2已及时正式受理，原command在原截止后重放时依H §7.2/9.4命中幂等历史，返回原版2/round2；不重新计时，不重新消耗机会。
4. **已受理后不同提交命令：拒绝符合约束。** H §7.2/9.4在新受理前检查当前终局，不新增材料版、轮次或成功受理；C不以网络重试名义产生version3。
5. **不同命令再次退回：拒绝符合约束。** 首次退回后无论仍待补证还是已进入round2，H §3.1/9.3及AT-07均禁止第二次退回；补证后判无效直接终结。
6. **已逾期后新补证：拒绝符合约束。** H §7.2终态及§9.4入口与C-05一致，材料不能重新受理，不开新Timer或补证机会。未套用P-20260904-04的未决补救。
7. **版2错误归属或材料不合法：不能成功受理。** C-01～03、H §9.4步骤2/3要求同Record且合法锁定；不把其他记录素材或非法包绑定round2，也不先把Timer标成已受理。
8. **C/B参与方提交失败：整体回滚边界一致。** H §9.4末尾及C-05要求同一逻辑原子结果，失败不能留下对外已受理、孤立round2或第三版；此项仅核对事务设计，实际锁/回滚未执行。

- **P1/G2与邀请：通过。** S-H §12.4逐条评审当前G2，无P0/P1概念不一致；执行前B/F恢复清单只引用Owner位置，G2-15A不复制它。GAP-H07/H19与E-02A/E-AT16的严格截止一致，等号拒绝；A分配和有效未计入口径未变。ISS-019/020/021在所述设计范围闭合，无新增业务PENDING。
- **P2：定点映射核对完成。** 本次变化涉及AT-05～07/20、AT-16；C的AT-19～21、E的AT-23～25及旧设计差异清单未改，沿用§8.2/8.8证据。没有重跑A算法、真实C/E流程、Contract verify或环境验证。
- **P1/P3文件检查：通过。** 本次仅Z主稿变化，19项受保护文件（指定业务/旧设计/交接/Contract、AGENTS/STATUS、四项DS、G2及两份H附件）哈希未变；125个编号结论块有来源标记，21项问题编号连续，34处本地链接/锚点可解析，空白/围栏检查通过。两段A算法SHA与既有证据一致；A正文除H版本链接、C除C-05R引用复核记录、E全部规则正文均未改，G2仍为已评审SHA。Git为指定main/HEAD，跟踪diff和暂存区为空；未做全仓扫描或重复执行稳定样本。

〔来源：S-H/S-H0差异及§3.1/7.2/9.3～9.5/12/17/18；S-C/S-E/S-BF/S-G2；AT-05～07/16/20；C-01～05/E-02A/E-AT16；本轮版本与文本比对输出。范围限设计Review，不替代业务决定、人类正式接受或运行验收。〕

### 8.10 接受登记与联合收尾检查（2026-09-06）

本轮只核对接受版本、状态登记及联合索引，不重跑§8.1～8.9已稳定的算法/流程样本。P0证据沿用；P1接受版本匹配，A/C/E正文逐字不变，G2业务边界除接受状态说明外逐字不变，18项其他输入哈希未变。联合汇总的文件清单、来源/锚点及格式检查记录在[联合增量汇总](p4-design-deltas-v81.md)§6，只有已实际核对的范围记为通过。产品、Contract重验证、数据库及恢复均NOT EXECUTED。〔本会话接受指令；Z-AUTH；§10.9；本轮逐字与哈希比对；任务§5。〕

## 9. 批次结束报告（供周润基联合收尾）

- **完成状态：PARTIAL / ACCEPTED（约定深度）。** 周润基已接受Z v9与G2：A达到深入级，C/E达到要点级，G2达到不少于半页的边界设计并有独立Review；本批次P0设计核对已闭合。当前进入联合收尾，完整Phase4 DoD、四项PENDING及运行验收不因此完成。
- **修改文件：** 本批次为主稿、`p4-migration-note.md`及联合收尾新增的`p4-design-deltas-v81.md`。本轮主稿v10/G2仅更新接受状态、来源记录和交接说明；联合汇总索引既有定义与证据，不复制算法、字段、计时或缺口定义。H附件只读，未编辑H正式文件、STATUS/Handoff或其他禁止路径。
- **本轮检查与真实结果：** P0算法及八项跨簇引用沿§8.1～8.9实际证据，本轮输入/定义未变，不重跑；P1核对接受版本与当前内容差异、来源/文件权限；P2沿用既有AT映射；P3检查联合汇总引用、格式及版本。联合收尾本轮结果见§8.10，运行检查均NOT EXECUTED。
- **未执行检查及原因：** 见§8.3，所有产品/Contract重验证/数据库执行NOT EXECUTED，未替上游或另一工作项作验证。
- **是否修改业务规则：否。** 未修改权威正文或新增业务决定；A-07/A-17/E-02A继续保留周润基明确答复，正式追溯仍需纳入。B资格冲突由B Owner修正，Z只更新引用与复核结论。
- **是否修改Contract：否。** 版本、状态、SHA基线保持；没有新增接口、枚举、生成物或CR文件。
- **旧API引用：** 只列现行operation-catalog影响定位，并引用STATUS说明仍有旧API；没有新增依赖旧API的实现，也未做全仓穷尽审计。
- **Mock：** 只有禁止猜测迁移/不能代替正式事实的说明，未创建Mock；既有Mock现状引用STATUS，未处理。
- **TODO：** 未以TODO遮掩交付；未完成内容明确列为PENDING、OPEN-OWNER和FOLLOW-UP，不宣称仓库没有既有TODO。
- **空接口：** 没有产品接口实现或空桩；本稿逻辑引用名和伪代码不是新Contract或可调用接口。
- **PENDING：** 既有P-20260904-01～04保持开放。ISS-019的邀请精度、ISS-020的B内部矛盾及ISS-021的评审范围缺项均已在设计范围闭合，无新增未决业务规则；没有新增BD或P编号。
- **P4-H概念引用：** 有效候选、补证计时、教师SLA、维护四个正式位置，以及B绑定C、客户端引用A、E引用B来源保护与G3/联合收尾边界，八项按§10.6确认对应。B修订后的受理条件已复核，G2获得精确版本定向Review；不复制H定义，不将本结论扩展为H整包或运行验收。
- **Phase3豁免：** 已登记负责人2026-09-06授权（由周润基转述）；无Handoff/Impact Matrix时用BD/AT/已提供业务交付内容替代，REQ级追溯留Phase3补做；未解锁Phase5。
- **待周润基决策/转交：** Z v9/G2接受及开始联合收尾已明确，无需重复确认。四项PENDING仍由负责人/相应Owner决定；请按联合汇总的H侧交接清单衔接正式文件落点、Handoff/STATUS和新汇总Review。AI未代发外部消息；H整包正式接受不由本次Z稿接受代替。
- **后续工作项：** 联合汇总已由Z成稿，H侧正式落点与交接仍待按职责完成；完整目录、性能/并发、恢复、客户端/Contract和追溯缺项继续按FOLLOW-UP及联合汇总登记。不得将“Z已接受”写成Phase4已DONE或Phase5已开放。
- **Phase4判断：IN_PROGRESS；phase_5_gate: LOCKED。** 所有成果的产品/数据库/迁移执行状态仍为 `NOT EXECUTED`。

## 10. P4-Z 问题与确认记录

> 本记录供周润基与黄友晟、负责人讨论，随本任务更新。首次登记：2026-09-06 21:08；本轮为2026-09-06联合收尾接受登记，v10；被接受的设计内容版本为v9。
>
> 状态：ACCEPTED（本批次约定深度）/ PARTIAL；产品与数据库执行：NOT EXECUTED；联合收尾进行中，Phase 4 IN_PROGRESS，phase_5_gate LOCKED。
>
> `P4Z-ISS-*` 仅是本任务的问题索引，不是新增BD、REQ、AT或业务规则。这里记录问题、证据、状态与答复，不复制§2/3/4中概念的正式定义。

### 10.1 使用与更新方式

收到答复或完成定向核对后，在同一问题下追加日期、答复原文/证据定位和状态变化；同步修改受影响设计段落，保留问题曾经存在的记录。只有AI建议时标“待确认”，只有设计核对通过时标“设计核对完成”，不替负责人或Reviewer做接受决定。若触及业务新疑问、既有PENDING阻塞、概念冲突或范围外写入，停止受影响部分并记入本节。持续更新指随本P4-Z工作和收到反馈更新，不自动监听其他对话或修改P4-H文件。〔来源：本会话“记录下来……并且不断更新，然后继续P4-Z”；原任务§4/5/7。〕

### 10.2 优先提交确认的事项

#### P4Z-ISS-001 · 跨簇引用与受影响项整改

- 级别/状态：P0；设计引用核对已闭合，8项对应；Z约定深度交付已接受，运行验收仍开放。
- 原始缺项：首轮OWN-02只有P4-Z逻辑别名；S-H0补齐正式位置后发现ISS-020，v8曾保留6项对应/2项挂起。以下带时间的记录保留历史，当前以本项状态和§10.6为准。
- 原影响：未取正式落点或B资格矛盾未统一时，不能关闭P0双向所有权核对；不妨碍独立算术与其他已确认设计。
- 当前答复：S-H §12.2/18.4逐项确认并修复ISS-020，§12.4完成ISS-021；Z按§8.9复核，不再等待这三项回复。
- 当前处置：OWN-02和§10.6只引用H正式位置，C-05R解除引用暂停。未改H文件，未将其整包或真实事务标为无冲突。〔S-H/S-BF；任务§4；OWN-02/03。〕
- 首轮进展（历史）：曾整理8项联合引用清单供周润基转交；清单本身不等于Reviewer答复，当时保持开放。
- 2026-09-06 21:47更新：周润基在本会话告知“我已经发给黄友晟了”，据此登记为已由周润基转交、等待答复；没有收到对方确认结果或定义位置，不关闭P0双向核对。〔来源：本会话周润基最新说明；§10.6。〕
- 22:53更新（历史）：截图消息为5确认/3待补，S-H0 §12.2为8确认；Z逐项核对后为6项对应、2项挂起，新增ISS-020交Owner统一，并未改写H的原回复。〔S-H0 §12；本会话截图；ISS-020。〕
- 23:33更新：收到S-H修订版，精确版本匹配；ISS-020八种最小流程文字核对完成，G2独立Review证据到位，8项设计引用对应，关闭本项引用缺口。接受与执行边界不变。〔S-H §12/18；§8.9/10.6。〕

#### P4Z-ISS-002 · 首次材料受理与期限边界

- 级别/状态：P0；既有 `P-20260904-02 / PENDING`。
- 影响位置：C-08、E-05/E-AT24及未来material/Record创建与受理事务；该分支未冻结。
- 需要负责人通过业务Owner明确：普通首次材料最晚时点；跨收尾和成员移出后的首次受理；服务器受理后传输终结条件。应补原业务决定，不能由C把游泳15分钟推广到普通运动。
- 当前处置：只设计已确认的材料版本、游泳规则及关闭前合法旧链；未补默认截止。提问对象为负责人，C/B/E根据答复分别消费。〔S-P；复核决定交接§4；总览§8/11.4/11.5；C-08。〕

#### P4Z-ISS-003 · 六类均不适用时的终局标准

- 级别/状态：P0；既有 `P-20260904-01 / PENDING`，正式审核概念归P4-H B。
- 需要负责人明确：补证后仍只有疑虑、六类均不适用或仅延迟例外条件不成立时，允许的终局判定依据是什么；不能把怀疑补写成已证实冒用，也不默认通过。
- 当前处置：A不判有效性，等待B提供已确认结果；C不增加原因或补证机会。该问题不由P4-Z替B关闭。〔S-P/S-BF；总览§12.2/12.3；PENDING-01。〕

#### P4Z-ISS-004 · 学校工作日日历与日内边界

- 级别/状态：P1；既有 `P-20260904-03 / PENDING`，正式计时概念归P4-H B。
- 需要负责人提供：学校统一工作日日历的权威来源、版本及“两个学校工作日”的日内计时边界，由B据此明确SLA口径。
- 当前处置：E仅引用B阻塞结果；不等同48小时，不自行用周一至周五。维护暂停事实仍归F，不能在此重写。〔S-P/S-BF；总览§12.4/18；PENDING-03。〕

#### P4Z-ISS-005 · 非维护故障造成错误逾期后的补救

- 级别/状态：P0；既有 `P-20260904-04 / PENDING`。
- 需要负责人明确：故障在错误逾期已经写入后才确认时，如何处理原判断与期限；由B/F统一相应事实和恢复语义。
- 当前处置：C/G2不以“技术恢复”重开真正逾期记录、不重置机会。已明确的维护暂停规则不替代本问题答案。〔S-P/S-BF；复核决定交接§4；G2-15/17；PENDING-04。〕

#### P4Z-ISS-006 · 正式Contract承载能力与既有阻塞

- 级别/状态：P1；正式归集位置已定位，Contract处置和新RC仍开放。
- 证据：本稿§6仅定位相关操作；`1.2.0-contract / RC`、`CR-20260901-005`以及最终成绩remark已登记缺口保持。没有通读schema，不能断言每个拟议字段已支持。
- 本轮已取得：S-H §17为正式归集位置，关联索引见CR-H-REF；规则/统计、材料、邀请/关闭/结算与既有缺口均有位置。本稿不复制第二份gap定义，不指定endpoint或wire值。
- 当前处置：设计名仅作逻辑概念；后续CR、版本发布与下游验证由相应Owner处理。〔Z-CONTRACT；CR-REF；STATUS“Contract当前基线与阻塞”；任务禁止路径。〕
- 本轮增量：S-H的GAP-H19已同步E-02A并关闭设计精度项，剩余表达归GAP-H07；GAP-H20/CR-005和其他正式Contract缺口保持，不因单项同步解锁Phase5。〔S-H §17/18.4；CR-H-REF；ISS-019。〕

#### P4Z-ISS-007 · Phase 3豁免后的追溯缺项

- 级别/状态：P1；豁免已登记，REQ/正式交付对应待补。
- 已确认：负责人2026-09-06授权可无Phase3 Handoff、无Impact Matrix进入本批次；无需重复审批该豁免。
- 需要联合收尾明确：所引用业务/Phase2交付内容对应的正式清单与快照；REQ级追溯由谁在Phase3补做、如何交接。未提供的编号不由AI生成。
- 当前处置：保留BD/AT/正文与旧设计来源；不因豁免解锁Phase5。〔Z-AUTH；任务upstream_handoff/impact_matrix；FOLLOW-UP。〕

#### P4Z-ISS-008 · 目标环境与既有数据证据缺失

- 级别/状态：P1；执行前证据待提供，不表示本轮发现数据库已有数据。
- 需要负责人/环境Owner在未来执行前提供：目标环境身份、只读盘点结论、事实来源以及备份恢复证据；若发现既有数据，先作迁移决定。
- 当前处置：本轮不连接环境；空库策略和 `DATA_MIGRATION_DECISION_REQUIRED` 只引用G2-03～15，不按名称、旧subtype或默认值猜测回填。〔S-G2；G2-03～15。〕

### 10.3 已确认事项（保留决策证据）

#### P4Z-ISS-009 · 认证与记录争用剩余额度

- 状态：已由周润基在本会话确认，已纳入A-07；本轮接受时保留本条原问答及§10.9的版本证据，正式REQ/业务交付对应仍待补。
- 原问题：本类目标600、认证570、选中记录q=60，逐条实际计入如何分配。
- 原文答复：“接受上述分配口径”。适用提案：认证先占类别额度，选中记录再按开始时间/稳定ID分配。
- 结果：A-07为唯一口径；没有新建BD号、修改正文或Contract。本条不再次定义公式。〔本会话第一项统计确认；A-07；S-A。〕

#### P4Z-ISS-010 · 有效未计入是否包括公式未纳入量

- 状态：已由周润基在本会话确认，已纳入A-17；原先较宽建议未采用。
- 原文答复：“仅统计可计分钟减实际计入”。因此90分钟有效运动、q=60、a=60的有效未计入为0；额外30不混入该列。
- 结果：A-17为唯一口径，E只引用；不能把原先建议误写成已接受。〔本会话第二项统计确认；A-17；S-A/S-E。〕

#### P4Z-ISS-011 · 开始存在未归属DS文件

- 状态：已解除预检阻塞，保留文件不动。
- 证据：最初发现根/`contracts/`/`docs/`三项 `.DS_Store` 后暂停；周润基答复“已确认，允许P4-Z继续”。首轮15项输入/既有文件哈希核对保持不变；本次续作Git状态一致。
- 当前处置：不清理、不加入Git、不覆盖。不是待负责人再次确认的问题。〔本会话预检与继续授权；§0；§8.2。〕
- 22:45续作授权：新增 `docs/architecture/.DS_Store` 后暂停，周润基明确答复“和此前三项一样，先不管，继续推进”。四项都保留不动；此授权解除本次预检阻塞，不授权其他Git写操作。〔本会话最新继续授权；§0。〕

### 10.4 P4-Z继续推进的工程核对项

#### P4Z-ISS-012 · 选择稳定性、重放与发布竞态

- 级别/状态：P0；本轮设计核对完成，Owner能力确认及实际运行仍开放；不要求新业务决定。
- 原缺项：首轮只覆盖总量与少数平局样例，未独立核对完整组合、输入顺序及原组合重算；来源令牌校验如何覆盖新增候选与发布竞态也未细化。
- 2026-09-06 21:13进展：72组完整组合与独立枚举一致，216次重排、72次同快照重算、54次移除已选记录后的对照通过；A-19A/A-22A～22C补齐集合版本、原子校验/CAS、前驱与恢复条件。A-08未改，详情见A-VERIFY-02/§8.4。
- 保留事项：同快照重算不等于已验证事件流或备份恢复；真实并发和性能NOT EXECUTED。本轮B设计保护已对应S-H §6.2，其他Owner保护协议和真实联调仍开放；不重复询问已定位的B来源位置。〔A-10/19/19A/22A～22C；S-A/S-H；旧设计§7.1；模块边界§4；本次设计核对输出。〕

#### P4Z-ISS-013 · 发布前可完成性需要真实时段见证

- 级别/状态：P1；条件样例与有限目录时间相容性已核对，完整目录生成和生产证据仍开放。
- 现象：仅有总可计日期容量不能保证两类均可完成，A-03的610/590反例至少需要21个日期；真实课程的允许时段、非重叠和学期约束还需逐项检查。
- 2026-09-06 21:13进展：A-PUBLISH-VERIFY构造7周、每周一/三/五各60分钟，共21日期；结果为相关11条、其他10条，总1200。少一个日期仅20个时最大1190。只证明明确假设下的样例，不宣称已验证真实课程参数或实现通用时间求解器。
- 2026-09-06 21:25进展：A-PLAN-01～04补充完整会话占用区间、跨周保留结束instant的有限目录参考算法，48组独立穷举对照通过。跨午夜冲突课程反例从忽略冲突时的1200降为实际目录上限1140，加入合法较晚时段才恢复1200；PAUSED占用、端点相接和替代项均有核对。目录不完整时找不到见证只能说明证据不足，不能判不可完成。
- 后续：真实许可日程来源、完整目录生成、时间离散化证明、数据规模和生产性能继续留Phase4，由正式设计接受后实施。不能把条件样例的“合法”假设当真实课程证据。〔S-R；AT-22；A-03/13；A-PLAN-01～04/VERIFY；FOLLOW-UP。〕

#### P4Z-ISS-014 · 工具调用包装与暂存缺失记录

- 状态：已修复；归属为工具调用文本包装，不是业务规则、环境或产品失败。
- 经过：一次Markdown反引号导致JavaScript解析失败，未写入；改字符串封装后成功。一次生成的Python正则字符串含意外换行，未完成检查；改为分段提取围栏后成功。
- 当前处置：后续避免混用多层模板字符串；两次之间有成功调用，没有同一种方式连续两次失败。若再次连续失败，按原任务停止、一次验证一个修复假设。〔首轮工具输出；§8.2。〕
- 本轮另一次：前轮暂存值不存在导致JavaScript构造参数时 `Object.keys(undefined)` 失败，尚未执行命令或写文件；改为显式列出本轮输入路径后成功。未连续尝试同一失效方式，不属于业务规则、数据样例或产品失败。〔本轮工具输出；§8.8。〕

#### P4Z-ISS-015 · 结算快照完整性与学生投影版本

- 级别/状态：P0；设计核对完成，Owner能力与真实事务仍开放；工程完整性问题，不是新增业务PENDING。
- 现象：准备结算时读取“无待办”和若干学生最新统计，之后来源可能变化；若准备成员清单与统计行同时漏掉同一人，两者数量相等仍会误当完整。少一名学生来源令牌也不能用默认值补足。
- 2026-09-06 21:25处置：E-08A～08C引用A的来源保护，增加与Owner完整成员集合的复核、每名成员来源令牌、投影归属/策略版本及报告前驱检查；冻结失败须重新准备，不能拼接不同时点结果或回切旧报告。E-10A补充归档消费完整课程结果、更正版保持历史范围引用。
- 证据：E-08D最终15种内存快照情形核对通过，覆盖未达标但无待办的正例，以及来源变化、漏人、重复、旧投影、部分读取失败等14项负例；不表示数据库结算已执行。
- 本轮B侧更新：完整审核阻塞与保护已对应S-H §6.2/6.3；其他模块的完整集合和提交参与仍各自确认。ISS-020已由B统一并经Z定向复核，E不重建B/F/D状态。真实并发、恢复与权限执行仍NOT EXECUTED。〔S-E/S-A/S-BF/S-H；AT-14/23/25；E-08A～08D/E-10A；A-22A～22C；§8.9/ISS-020。〕

#### P4Z-ISS-016 · 性能优化与提前停止不能丢失平局规则

- 级别/状态：P0；新增反例和公式核对完成，真实容量评估仍开放；不是已发现产品故障。
- 现象：首次找到1200就结束搜索、同日只留最长记录，都可能得到正确总量但错误的选中ID。当前A-08没有使用这两种错误优化。
- 本轮处置：A-SCALE-01～03区分状态数、位序/见证存储与实际运行预算，明确资源中止不产生正式精确结果；A-SCALE-VERIFY两组反例核对完整选择而非只比总量。
- 后续：实现Owner需给出保留原组合及平局次序的优化证明、规模分布与预算证据，未获授权前不执行产品性能测试。无需新增业务决定；AT-10/13既有顺序继续适用。〔S-A/S-R；A-08/10/13；A-SCALE-01～03/VERIFY；AT-10/13/22；本轮计算输出。〕

#### P4Z-ISS-017 · 恢复不能仅凭总量相等判定完整

- 级别/状态：P1；统计反例与恢复边界评审完成，环境证据/演练仍开放；不表示已经发生数据丢失。
- 现象：相同最新候选和认证可因合法原组合不同产生不同选中ID；缺少A检查点时重算总量仍为1200，却可能更换原组合。历史报告、材料对象和B/F计时来源同样不能只靠总数或数据库URL证明完整。
- 本轮处置：A-RECOVERY-VERIFY核对两条历史与恢复差异；G2-15A～15D补证据清单、恢复顺序和八项评审情形。G2只引用A/C/E及B/F原定义，没有新建计时/改判语义或回填映射。
- 后续：环境Owner提供恢复点与完整性证据；B/F所需恢复事实的唯一清单已由S-H §12.4指明，G2只引用，不复制。真实备份恢复、事件重放、媒体检查及恢复上线均NOT EXECUTED；必要事实缺失时不猜测补齐。〔S-G2/S-A/S-BF/S-H；A-19A/A-RECOVERY-VERIFY；G2-12～15D；AT-13/19/23/25；ISS-008/021。〕

#### P4Z-ISS-018 · 未计入解释的分钟范围与自占名额

- 级别/状态：P1；10种定向情形已核对，本批次设计交付已接受；客户端/Contract表达与真实执行仍开放。
- 现象：若把m-q与q-a混为一项，90分钟全部可计量已计入也会被误说为有效未计入30；若部分计入时直接检查周总条数，又可能将本类截断误归因为该记录自己占用的名额。
- 本轮处置：A-15A～15D只在统计唯一正式位置补解释范围、当前组合限制与异常判据。8项正常解释和2项注入不一致得到预期结果；可生成原因本身不证明组合最优。
- 后续：E/客户端应按A引用这些解释，Contract Owner决定正式表达方式；不新增审核原因，不把待处理/无效映射成有效未计入，也不重新确认已经接受的q-a口径。〔S-A/S-BF；A-14～17；AT-09～14；总览§11.3/15/21；本轮内存核对。〕

#### P4Z-ISS-019 · 邀请宽限结束时刻的等号

- 级别/状态：P1；设计精度与H/G3同步已闭合；Contract实现仍归ISS-006。
- 发现来源：S-H0 §12.3 finding 6、GAP-H19及22:33截图；旧E-AT16有12:39:59/12:40:01，却未写12:40:00。
- 本会话答复原文：“采用严格早于，截止时刻拒绝（推荐）”。E-02A成为唯一正式设计位置，E-AT16已补等号；8项时间谓词核对通过，不重写业务正文或Contract。
- 23:33闭合证据：S-H §12.3 finding 6、GAP-H07/H19和§18.4已同步E-02A/E-AT16，截止等号拒绝，与周润基决定一致。未重跑未变的8项时间谓词；无需再次征询端点，不能外推其他未决首次材料期限。〔S-E；AT-16；E-02A/E-AT16；S-H上述位置；本会话答复。〕

#### P4Z-ISS-020 · B补证资格的相反条件（P0）

- 级别/状态：P0；B Owner已整改，Z定向文字复核通过，设计finding闭合并解除引用项2/5暂停。非运行缺陷修复或产品验收。
- 原位置：S-H0 §3.1使用单一“是否已经使用补证机会”/`supplementUsed`；§9.3退回后已用、§9.4步骤2要求已用，§7.2却要求未用。
- 原最小反例：首次合法退回后待补证且标记已用，学生期限前提交合法版2；S-H0 §9.4的已用与§7.2的未用不能同时满足，Z因此暂停受影响引用，没有自行拆机会或反转判断。
- 影响处置：C-04/05始终只消费B公开结果；原问题未通过放开第三版、重开逾期或省略B资格绕过。C-05R当前只登记Owner修订及复核结论。
- 23:33 Owner整改与Z复核：S-H SHA `b4baad0330bc7c6008566dc66394791e926ed21846a800d9bc6bcf04f9dd3bae`，§3.1（行97～108）、§7.2（行299～301）、§9.3～9.4（行407～423）已统一；唯一正式语义留B，未形成第二次机会。§8.9的8项最小流程逐步核对符合C-04/05，引用项2/5解除暂停。没有实现或运行B状态机，不重复未变化的B全量检查。〔S-H上述位置及§12.3/18.4；S-C/S-BF；AT-05～07/20；C-05R；任务§4。〕

#### P4Z-ISS-021 · G2独立文件未进入H评审范围

- 级别/状态：P1；精确版本独立Review证据已到位并核实，本评审范围缺项闭合；环境和恢复执行证据另按ISS-008/017开放。
- 原证据：S-H0 §0/12.1/12.3 finding 3/18.2明确未收到迁移稿，因此v8未把Z自述当作G2独立评审。
- 已评审文件：[p4-migration-note.md](p4-migration-note.md)，76行、15101字节，SHA-256 `55b04e2c198ebe7b824686b9a561c2879aade4bd6510115c71c7bf1add00e6d4`；本轮未改，仍PRE_DRAFT/NOT EXECUTED。
- Review范围与结果：S-H §12.4对G2-10/12～15D（重点15/15A/15C）逐项确认，并说明其他边界未发现违反H定义；没有把技术恢复当重开计时/补证，也没有覆盖A检查点或E历史报告。H结论为无P0/P1概念冲突，Z已核对所指G2段落与版本。
- 23:33闭合证据：S-H §0/12.4/18.2/18.4明确列出上述G2完整SHA和独立Review；执行前B/F恢复清单唯一引用[H §12.4](p4-design-huang-v81.md#p4h-z-g2-review)。此清单要求仍留后续执行方案，未在G2复制，也未由AI连接环境或执行恢复。〔S-H/S-G2；任务G2/路径边界。〕

### 10.5 更新流水

- **2026-09-06 21:08，v1：** 按周润基要求建立问题记录；登记14项，区分待确认、既有PENDING、已确认和工程核对，不重写整份设计。续作Git根/分支/HEAD一致，新增文件仍仅P4-Z两份；开始推进ISS-012/013。
- **2026-09-06 21:13，v2：** ISS-012补齐完整组合与稳定性对照、原子发布/恢复设计；ISS-013补齐AT-22的20/21日期证据；追加四组已确认报表口径核对。没有新增业务PENDING、修改算法或宣称P4-H确认，所有运行状态仍NOT EXECUTED。
- **2026-09-06 21:14，v3：** 定向Review补明幂等返回旧检查点不代表来源仍当前、不得回切指针；文件复核通过（99个编号来源块、14项问题、21处链接），15个受保护输入和迁移稿保持不变。ISS-001～008等待相应确认/证据，ISS-009～011和014已确认或修复，ISS-012/013本轮证据完成但完整工程门禁仍开放。未执行Git写操作、产品代码、Contract或数据库修改。
- **2026-09-06 21:27，v4：** 继续ISS-013，新增有限计划目录时间相容性算法、48组对照和冲突反例；新增ISS-015并完成15种结算快照核对。问题记录增至15项；A-08保持原定义，C/E继续只引用B/F事实。文件复核通过（109个编号来源块、21处链接，15项输入/既有文件及迁移稿未变）；真实课程目录、事务运行与跨Owner确认仍开放，详见§8.5。
- **2026-09-06 21:37，v5：** 新增ISS-016/017，问题记录增至17项；补齐规模公式、两组错误优化反例、历史选择恢复反例及G2恢复证据清单。两段参考算法未改，本轮有意更新迁移稿§5.1；文件检查通过（118个来源块、21处链接，15项只读输入/既有文件未变）。数据库、性能与恢复上线仍NOT EXECUTED，详情见§8.6。
- **2026-09-06 21:43，v6：** 新增ISS-018，问题记录增至18项；A未计入解释的10种情形核对完成，§10.6整理8项联合引用确认清单。仅修改主稿，不复制B/F定义、不替对方确认；文件复核通过（122个来源块、21处链接，15项只读输入/既有文件及迁移稿未变），详情见§8.7。
- **2026-09-06 21:47，v7：** 根据周润基“我已经发给黄友晟了”登记8项清单已由本人转交，等待黄友晟答复；18项问题数量不变。当前为四项约定深度草稿已成稿、待交叉Review和正式接受；本次仅更新协作状态，未新增算法计算或重跑旧检查，Git基线和状态复核一致。
- **2026-09-06 22:58，v8：** 接收截图及S-H附件；修改前Z SHA与H评审对象匹配。8项正式位置已找到，Z核对6项对应、2项受新增ISS-020阻塞；新增ISS-019邀请端点已获周润基严格早于的答复并完成8项时间核对；新增ISS-021登记G2独立Review缺失。问题记录共21项。第四项DS已获保留继续授权；只改Z主稿，H和G2保持原输入。文件复核通过（125个来源块、29处链接、18项受保护文件哈希未变），详见§8.8；转交反馈见§10.7。
- **2026-09-06 23:33，v9：** 接收H修订版S-H，其版本和已评审Z v8/G2身份均匹配。定向文字核对B整改及8种最小流程，8项跨簇设计引用对应；关闭ISS-001引用缺口及ISS-019/020/021，问题仍共21项。仅更新本主稿的引用、状态和Review证据，不修改G2、H、算法、业务或Contract；正式接受、四项PENDING、后续工程和Phase5门禁保持。检查与闭合回执见§8.9/10.8。

- **2026-09-06，v10：** 周润基接受Z v9与G2并启动联合收尾；登记被接受版本的完整SHA，主稿/G2仅更新接受与交接元数据，Z唯一编辑联合汇总。既有21项问题保留，四项业务PENDING不变；新汇总待联合Review，H侧正式文件与Handoff/STATUS仍按职责处理。详情见§10.9/8.10。

### 10.6 联合引用逐项答复与Z核对结果

当前证据：S-H精确修订版与23:25截图均回复8确认/0不一致/0待补；旧截图与S-H0、v8暂停过程保留在§8.8/ISS-001/020。Z已复核改变的资格条件及G2独立Review，并确认原有效候选/来源保护、SLA/F引用、维护与G1统计引用段落逐字未变；当前8项设计引用对应，不代表产品/事务验收。〔S-H §12/18；本会话两次截图；任务§4；OWN-01～03；§8.9。〕

1. **有效候选：设计引用对应。** H §6.1/6.2对应A-05、A-14/15A、A-22A；候选和来源保护只引用B，时长/日期仍取Record/Session Owner，不复制候选判定。
2. **补证计时：设计引用对应，暂停解除。** H §3.1/7.2/9.4对应C-04/05、E-07/08；ISS-020条件已由B统一并经Z定向核对。C不复制机会字段或Timer算法。
3. **教师SLA：结构与引用对应。** H §7.3/6.3对应C/E的轮次与阻塞消费；P-20260904-03日历仍未确认，不据此宣称真实dueAt可用。
4. **维护暂停：来源与独立G2 Review对应。** H §7.4/15.5对应C-04及G2依赖；H §12.4已评审G2精确版本。G2只引用Owner恢复清单，真实来源与恢复演练仍未执行。
5. **B绑定C材料版本：设计引用对应，暂停解除。** H §2.2/3.1/9.4引用本稿§3.1/C-03/05；C负责材料事实、B负责轮次与Timer，同一次受理与原命令重放不增加材料版或机会，见§8.9。
6. **E/客户端引用A统计：设计引用对应。** H §2.2/14.6/16.5指向本稿§2.5；分列、未计入原因与原组合规则留A，H不复制算法。A-07/A-17既有确认保持。
7. **结算与来源保护：B侧设计能力对应。** H §6.2/6.3/9.5对应A-22A～22C与E-08A～08C；只保证审核来源与保护到提交，其他成员/名单/媒体/Session/申请/统计由各Owner提供。实际事务未执行。
8. **Contract与联合收尾：正式引用位置对应。** H §17/18对应CR-H-REF与本稿§9。H19已同步并归入H07，G2精确Review已完成；Contract发布、正式接受与Phase5总门禁保持原状，未写Handoff/STATUS。

上述只记录引用关系，不形成第二份正式定义或缺口清单。其来源沿用S-H各对应节、S-A/S-C/S-E/S-BF及模块边界§2.1/4/6；后续只核对Owner修改的条件和受影响引用，不重跑稳定算法样本。〔任务§4/5；§10.1；ISS-019～021。〕

### 10.7 v8转交黄友晟的三项反馈（历史原文，已获回复）

1. **请统一P0补证资格条件（ISS-020）。** 在H附件SHA `4f8de363215e2ed844ecc386b485e274c7d42a18d32a7e289c1556f5a473fd87`中，§9.3首次退回标记机会已用、§9.4步骤2要求机会已用，§7.2却要求机会未用。Z暂停关闭补证计时及材料绑定两项；请由B Owner统一并返回新SHA/变化位置。Z未改H文件，详细最小推演见ISS-020。
2. **Z/E邀请端点已补（ISS-019）。** 周润基已明确选择 `accepted_at < expires_at + 10分钟`；等于截止时刻拒绝。E-02A/E-AT16已更新，8项时间谓词核对通过；请同步H的GAP-H19并仅复核E相关变化。此决定不外推其他业务期限。
3. **请补G2独立定向Review（ISS-021）。** 需同时提供本主稿新版本及 `p4-migration-note.md`。G2本轮未改，精确SHA `55b04e2c198ebe7b824686b9a561c2879aade4bd6510115c71c7bf1add00e6d4`；重点G2-10/12～15D，尤其15/15A/15C。此前H评审的旧Z SHA已与本轮修改前文件匹配；本次新Z的最终SHA随交付消息给出，不能继续把旧SHA视为已包含新改动。

以上保留v8转交反馈历史；周润基已转交并提供回复，当前处理结果见§10.8，不再作为待发请求。AI没有代发外部消息，也未执行Git、Contract或运行操作。双方定义位置不变，成果仍PRE_DRAFT/PARTIAL/NOT EXECUTED，Phase4 IN_PROGRESS、Phase5 LOCKED。〔S-H0 §12/17/18；S-H §18.4；E-02A；ISS-019～021；本会话答复；任务权限边界。〕

### 10.8 v9闭合回执（历史交付，接受登记见§10.9）

已按H修订附件 SHA `b4baad0330bc7c6008566dc66394791e926ed21846a800d9bc6bcf04f9dd3bae` 完成定向复核：

1. **ISS-020：设计finding闭合。** §3.1/7.2/9.3～9.4已统一；Z的8种最小流程核对完成，补证计时与B绑定C两项暂停解除。Z只保留正式引用，没有复制B机会/计时定义。
2. **ISS-019：设计精度及G3同步闭合。** H的GAP-H07/H19与E-02A一致；等于宽限截止拒绝。剩余Contract工作归H07，不改变Phase5总门禁。
3. **ISS-021：独立Review缺项闭合。** H §12.4覆盖G2 SHA `55b04e2c198ebe7b824686b9a561c2879aade4bd6510115c71c7bf1add00e6d4`；G2保持原文件，执行前B/F清单仅引用H该节，真实恢复证据仍留后续。

当前八项H↔Z设计引用对应；Z主稿v9仅吸收本轮答复、引用和核对记录，最终SHA随交付消息提供。无需为已关闭的三项重复等待同一答复；若后续有实质设计改动，再提供精确版本及变化范围Review。周润基正式接受和联合收尾仍待办理，四项业务PENDING保持开放，成果PRE_DRAFT/PARTIAL/NOT EXECUTED；Phase4 IN_PROGRESS，phase_5_gate LOCKED。〔S-H §12/17/18.4；§8.9/9；ISS-001/019～021；任务§4/7及路径门禁。〕

### 10.9 周润基接受与联合收尾登记（2026-09-06）

- **接受证据：** 周润基在本会话明确：“审核完毕接受草稿，现在开始和黄友晟联合收尾”。按上一轮交付对象登记Z v9及G2的约定深度设计为ACCEPTED；不扩展为H整包接受、完整DoD或产品/Contract/数据库执行授权。
- **被接受版本：** Z v9为821行、132870字节，SHA-256 `d3f5b911fad2afdcca5d5476765316d0eb9da77feeac554b4fd7d3f343229a46`；G2为76行、15101字节，SHA-256 `55b04e2c198ebe7b824686b9a561c2879aade4bd6510115c71c7bf1add00e6d4`。联合收尾预检逐项匹配；S-H仍为§1.1的`b4baad03…d3bae`。
- **本轮改动边界：** 主稿v10与G2仅登记接受及当前交接状态；A/C/E算法、材料与业务规则未改。旧PRE_DRAFT、未进入联合收尾等描述保留于有版本/日期的历史记录，不作为当前状态。G2初轮Review及接受所指SHA仍保留，不能把接受登记后的新SHA冒称旧Review附件。
- **联合编辑：** [联合增量汇总](p4-design-deltas-v81.md)由周润基侧唯一编辑；新汇总是PRE_DRAFT、待联合Review，本次既有草稿接受不预先接受其新内容。H正式设计/分层/缺口文件及`docs/rebuild/handoffs/new-req-phase-4.md`、`docs/rebuild/STATUS.md`仍由H侧按职责处理，Z不写入。
- **当前交接范围：** 不重新等待ISS-019/020/021；联合汇总记录八项既有确认及H侧正式落点/交接任务。对方附件中的分支或建议不改变本任务main基线及禁止Git写操作的授权边界。四项PENDING继续开放，批次PARTIAL，Phase4 IN_PROGRESS，Phase5 LOCKED。

〔来源：本会话接受/联合收尾指令；原任务§1/4/7/8允许路径与验收深度；S-H §12/18；本稿§8.9/9及ISS-019～021；本轮Git/哈希预检。〕

### 10.10 文档归档与 PR 发布登记（2026-09-07）

周润基转述负责人已授权提交 PR，并确认黄友晟同意由周润基侧统一提交。具体范围见[联合汇总 §10](p4-design-deltas-v81.md#10-文档归档与-pr-发布授权2026-09-07)。本主稿归档前接受登记版为837行、135717字节，SHA-256 `8eb159ddb9235ef29a1ead162e0702b5795c2eefd423c325f1ae2058ccfcaafe`；本次仅修正H正式稿链接及归档/授权元数据，A/C/E定义和算法不变。历史附件SHA继续标识当时评审对象，当前六份仓库文件由Git提交与路径定位。

P4-Z/G2、H整包及联合汇总均已接受为本批次PARTIAL设计交付；四项业务PENDING保持。Phase4 IN_PROGRESS，Phase5 LOCKED，产品/Contract/数据库/migration/恢复仍NOT EXECUTED。〔来源：本会话负责人授权与统一提交指令；联合汇总§9～10；本稿§10.9。〕
